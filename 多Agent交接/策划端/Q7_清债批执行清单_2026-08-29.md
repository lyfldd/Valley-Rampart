# Q7 清债批执行清单：DZ-021 补提交 + 种子① Lumbermill 资产清扫

> 签发：策划端（2026-08-29，Q6 追加裁决 C 方案=用户拍板「全清+资产扫」后推主线）。
> **锚点**：主仓 @ C 面清债 commit（本清单签发时的 HEAD）；执行前以 `git log -1` 实测为准。
> **总纪律**：本批允许改动面 = 仅 `Module_Production.asset`（T3）+ HH 交付文件。其余产品代码/资产/设计文档**零改动**。红即停、报策划、不自修、不虚报（HH.37 铁律）。
> **衔接**：本批完成 → 立即转主线清单《Q1_步骤12全量收官回归执行清单_2026-08-29.md》，从其 **T2 编译门**开始（其 T0/T1 已被本批 T1/T2 覆盖）。两批合并一份 **HH.39** 交付报告：§一=清债批（本清单），§二=主线收官（主线清单 T2~T9）。

---

## §1 任务总表

| 编号 | 任务 | 类型 | 依赖 | 出处 | 验收标准（验收句） | 证据 |
|------|------|------|------|------|-------------------|------|
| T1 | **git 环境预检**：实测 `git status`/`git log -1` 正常 | 流程 | — | HH.37 R5 教训 | status 输出正常、无滞后报错 | `git status --short` 输出截录 |
| T2 | **DZ-021 补提交（=Q7 原任务）**：只 `git add "河谷防线开发计划书具体内容/改造计划/2_17_AI王国脑与自主成长.md"`（工作区 3 处一并入库：D325 作废注/D337 D400/SimModeConfig 视野 D344 行），commit+push。**禁 add 其他 8 份在途文件** | 流程 | T1 | Q6 报告 §三 DZ-021/§七#1 | HEAD 含「D400 取代原 D279」+「D414 作废注」（D325 行）；`git diff HEAD --stat -- <2_17设计>` 为空 | `git grep -n "D400 取代原 D279" HEAD -- <2_17设计>` + 空 diff 输出 |
| T3 | **种子① Lumbermill×3 死数据清扫**：①先读 `Assets/Resources/Modules/Module_Production.asset` L15-45 确认三行 `- Lumbermill` 所在字段上下文（预期=模块 allowedBuildings 类列表）；②删除该 3 行；③全库复查 `git grep -i lumbermill -- "Valley Rampart/Assets"` 应仅剩零命中（报告口径：全库唯一引用面）；④编译 0 error；⑤Play 冒烟（GameScene 真开局）：生产模块建筑链/采集链/建造菜单零回归 | 资产 | T1 | Q6 报告 §二种子①/§七#4 · 用户拍板提前（原挂步骤14） | ①`git grep -i "lumbermill" -- "Valley Rampart/Assets"` 零命中 ②编译 0 error ③Play 开局采集/建造/模块面板无异常、无死条目 | asset diff（仅 3 行删）+ grep 输出 + 编译/Play console 截录 |
| T4 | **收尾**：HH.39 §一（清债批结果）落盘 `多Agent交接/执行端/`（§二留待主线完成后补）+ `_交接索引.md` 登记 HH.39 行（勿漏登）+ git-plan-sync 工作日志插行；共享文件写完**立即同串 commit**（只 add 自己文件：Module_Production.asset + HH.39 + 索引 + 工作日志） | 流程 | T2、T3 | vr-triage-flow §九 · HH.37 点名 | 索引含 HH.39；commit 构成与报告一致 | HH.39 文件 + `git show --stat HEAD` |

**T3 边界裁决预授权**：若步骤①发现 Lumbermill 存在本清单外的其他引用面（与 Q6 报告「全库唯一」口径冲突）→ **停**，不改资产，HH.39 写明发现报策划重裁。若字段上下文与预期不符但仍是死列表项 → 可删，diff 里写清上下文。

## §2 禁改清单

1. 产品代码 `Assets/_Game/**` 零改动（本批无任何代码任务）。
2. 资产仅 `Module_Production.asset`；**其他 SO/资产零触碰**（Academy 退役仍挂步骤14，勿顺手做）。
3. 文档只允许写：HH.39 §一、_交接索引 HH.39 行、工作日志插行。**2_17设计 只 add 不改**（T2 只提交已存在的工作区改动）。
4. 队列只读；状态由策划端验收后转移。

## §3 停机判据

- T1 git 异常 → 全批暂停先报环境。
- T3 编译错/Play 异常/Lumbermill 出现意外引用面 → 保留现场停，HH.39 报告。
- 停机后不自修产品码，归因按「让渡三问法」推到真帧同条件再下结论。
