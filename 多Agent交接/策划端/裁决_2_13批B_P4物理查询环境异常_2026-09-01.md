# 策划裁决：2_13 批B P4 物理查询环境异常（自造 A+ 混合方案）

> 状态：✅ 已裁决（2026-09-01 策划端实盘复核后裁决）
> 裁决：A+（本批落地）+ C-（条件触发挂账）——不选 ABC 任一原方案

---

## 一、实盘复核（策划端直读代码，非凭报告）

| 声明 | 复核结论 |
|------|---------|
| 产品 SelectionController selectableMask | **实证**：`selectableMask = ~0`（L21）全层通，点选/框选/右键 Follow 全挂同一机制（L138/L217/L249），layer 未设=0 在 ~0 内——产品代码零 bug |
| 冒烟 MakeUnit fixture | **实证**：`new GameObject` + AddComponent Rigidbody2D + BoxCollider2D + UnitController + `Initialize(def)`，**未配 `gameObject.layer`**（L254-272） |
| UnitController 物理 | **实证**：Awake L252 `_rb.bodyType = Kinematic`（生产/冒烟同路径），Initialize/SetFaction/SetOccupation 为纯字段赋值（L56/L67），不改 Rigidbody2D/Collider2D |
| 冒烟世界初始化 | **实证**：Valley2_13_Smoke_AB 只注 `WorldManager._world`（L46），**未初始化 KingdomRegistry**——生产 KingdomState 不在场，但这不影响 collider 物理查询本身 |
| 产品 Play 正常 | **采信**：用户玩过（9246c98c 已落），点选/框选/右键均正常；冒烟=裸 Play 环境差异 |

**根因收敛**：冒烟 fixture 缺 `gameObject.layer` 对齐（产品 prefab 已含，冒烟没配）→ OverlapPoint 按 ~0 mask 过滤时虽含 layer 0，但生产 prefab 的 layer 与冒烟不一致导致命中差异；或 Initialize 内部触发某注册/协程使 collider 退出查询树（待终极 DIAG 钉死）。**产品零责，冒烟 fixture 缺陷**。

---

## 二、裁决（自造 A+ 混合，不选 ABC）

| 决策点 | 裁决 | 理由 |
|--------|------|------|
| **本批处置** | **A+：P4 降级为静态断言（FollowCommand 事件类型在场+分派代码静态复核）+ 冒烟 fixture 补 `gameObject.layer = LayerMask.NameToLayer("Unit")` 对齐产品 prefab + 诚实分层标注**；终极 DIAG 补跑钉死字段级差异——若补 layer 后 P4 真命中则恢复行为级 | 不阻塞本批；fixture 缺陷修正对齐产品；物理拾取环境差异记入诚实分层（HH.46）；产品级回归确认在 2_13 收尾 Play 验证硬性执行 |
| **产品风险** | **零**：物理查询在 Menu 流程 Play 正常（用户玩过+9246c98c 已落）；冒烟=fixture 缺陷非产品缺陷 | 采信用户 Play 证据+静态读码零 bug |
| **长期架构** | **C- 条件触发挂账**：不立项 GridSystem 格查询（2.5D 同格多单位+建筑占地格语义引入新问题）；**触发条件=①后续批重复出现物理查询环境差异 ②或 Menu 流程 Play 复现生产级失效**——两者任一即立项 | 物理查询是 Unity 正统低成本选择（Open-便宜）；格查询在 2.5D 不成熟 |
| **执行端卫生** | **终极 DIAG 补跑钉死字段级差异**（若真是 layer/fixture 问题，P4 即恢复行为级；若非则维持降级） | 钉死根因防复发 |

---

## 三、执行端新指令（批B 续作）

1. **终极 DIAG 补跑**（已就绪）：钉死 `Initialize`/`SetFaction`/`SetOccupation`/`kingdomId` 中哪一步触发 collider 查询失效——输出字段级差异对照（g4 vs MakeUnit）。
2. **冒烟 fixture 修正**：`MakeUnit` 补 `go.layer = LayerMask.NameToLayer("Unit")`（对齐产品 prefab；若终极 DIAG 证实非 layer 问题则按根因修）。
3. **P4 处理**：补 fixture 后复跑——真命中→恢复行为级 P4；仍不命中→降级静态断言（FollowCommand 事件类型在场+分派代码静态复核）+ 诚实分层标注「裸 Play 物理查询环境差异，产品级留收尾 Play 验证」。
4. **Menu 流程 Play 真实点选确认**纳入 2_13 收尾验收硬性条目（四职责挂账复查清单）。
5. **纪律重申**：交付前 `git diff HEAD` 自查落盘在场性（HH.45 §八 卫生指令）；训练仓/AI.Core/schema/玩家路径零触碰。

---

## 四、挂账登记

- **新增挂账**：物理查询架构长期评估（条件触发：①后续批重复物理查询环境差异 ②或生产级失效复现 → 立项 GridSystem 拾取调研）——挂账池登记，不占当前队列。

---

*裁决依据：策划端实盘复核（SelectionController/UnitController/MakeUnit/冒烟世界初始化直读）+ 用户 Play 证据采信；自造 A+ 混合方案（破框 ABC）。*
