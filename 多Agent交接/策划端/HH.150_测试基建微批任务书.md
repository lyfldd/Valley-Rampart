# HH.150 测试基建微批任务书（存量裸局容器迁移正门）

> 签发：策划端（D600，2026-09-10）· 执行：执行端（TraeCode·Unity 轨）
> 依据：D600 存量关联列报裁决（0.6 §一百二十九）· L-17 变体③+防范动作补记（存量容器全量 grep 清单化）
> 取号：HH.150（读 _编号登记.md 直取；完成报告=**HH.151** 同笔预留）
> 排期：批E 之后（测试基建收口）；与批E 触碰面正交但 2_20B 回归需协调（见 §四）
> **本批相关教训引用（钩子1 排雷）**：**L-17**（容器入口纪律+变体③守卫覆盖期≠副作用无害期+**存量容器补改完整性补记**）/ **L-13**（编译双通道）/ **L-16**（协程捕获器+Play 态重入全清）

---

## 一、背景（D600 实锤）

SmokeApi.EnterGame 裸局存量 **9 处**（L-17 入库时「存量容器补改」只覆盖 HH.107 未全量扫描=策划端签发侧完整性缺口）：

| # | 容器文件 | EnterGame 行 | 备注 |
|---|---------|-------------|------|
| 1 | Valley2_20B_Smoke_M7.cs | L90 | 六轮回归容器（本次六轮侥幸零判负，历史批B/批C 同险） |
| 2 | Valley2_20C_Smoke_M8M9.cs | L154 | 抽象经济回归 |
| 3 | Valley2_20_Smoke_Race.cs | L77 | 种族回归 |
| 4 | Valley_HH73_Smoke_Water.cs | L49 | 供水链（旧容器） |
| 5 | Valley_HH76_Smoke.cs | L40 | 零碎小批 |
| 6 | Valley_HH78_Smoke.cs | L41 | 零碎小批 |
| 7 | Valley_HH80_Scout.cs | L43 | 侦察（HH80_Run 已是正门） |
| 8 | Valley_HH81_Smoke.cs | L44 | 零碎小批 |
| 9 | Valley_HH89_Smoke_Measure.cs | L40 | 统一度量衡 |

已正门容器（不属本批）：HH107/HH109/HH111/HH115/HH128/HH137/HH140/HH144/HH80_Run/TestHarnessDemo/P0_Diag。

## 二、实施口径（D600 裁定）

1. **9 处裸局容器迁正门**：`SmokeApi.EnterGame(cfg)` → `TestHarnessApi.EnterTestRun(cfg, speedOverride)` + 收尾 `ExitTestRun`（L-17 容器三态模板按各容器语义选态：回归类=真实局态，探针类=守卫玩家态）。
2. **或至少**：ExitTestRun 后强制退 Play 防跨容器状态纠缠（若个别容器结构上不适合全量迁正门，须列报理由，默认全迁）。
3. 迁改不改变容器探针断言逻辑本身（只改入口+收尾），防止回归语义漂移。
4. **全量 grep 清单化**：迁移完成后 grep 全仓 `SmokeApi.EnterGame` 确认零裸局残留（除 TestHarnessApi.cs 内部实现外），证据入报告。

## 三、验收句（探针义务，缺一退回）

- 9 处容器全部迁正门或列报豁免理由；
- 迁移后全量重跑：2_20B 六轮+2_20C+2_20_Smoke_Race 全 PASS（回归语义不漂移）；
- `SmokeApi.EnterGame` 全仓 grep 零裸局残留（除 TestHarnessApi.cs 内部）；
- 每容器收尾 ExitTestRun+（如适用）强制退 Play 实锤日志。

## 四、与批E（HH.148）协调

- 触碰面正交（HH.150 改存量容器入口/收尾，批E 新建正门容器），**并行不互阻**；
- 但批E E4 回归依赖 2_20B/2_20C——**若批E E4 回归在 HH.150 之前跑，则 2_20B 仍是裸局状态**（可接受：批E E4 按当前状态回归，HH.150 完成后补跑一轮 2_20B/2_20C 迁移验证）；执行端统筹两者时序并列报说明。

## 五、红线承诺

AI.Core 零直改 / 玩家侧零改动 / 阶段机零触碰 / 容器探针断言逻辑不改（只改入口收尾）。

## 六、交付序

逐容器迁移（按 §一 表序）→编译双通道（L-13）→全量重跑回归→grep 零裸局残留取证→HH.151 完成报告→commit+git-plan-sync→策划端验收。

执行端接单开工（批E 后）。
