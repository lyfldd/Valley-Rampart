# HH.94 训练端 2.0 世界模型改造（W 批）实施清单

> 签发：策划端 2026-09-07 · 配套：HH.94 任务书（同日，D550）· 接单：训练师会话（**TraeCode=主指挥 + opencode=施工手**）
> 定位：任务书管「做什么/为什么」，**本清单管「怎么一步步做、哪里有雷、怎么算做完」**——逐条执行、逐条留证、状态写回本清单回执区。
> 冲突序：训练仓 AGENTS.md > HH.94 任务书 > 本清单。
> 用法：每完成一条把 ⬜ 改 ✅ 并附证据指针（commit hash / 命令输出）；全部 ✅ 才许交 HH.95。**排雷图 M1~M16 开工前必须通读一遍。**

---

## 一、P0 开工前置（4 条，全过才许动代码）

- ⬜ **P0-1 基线与存档点**：训练仓 `git status --short` + `git log --oneline -3` 贴报告；干净则直接打存档点 commit（message=W批开工基线）；有未提交改动先列报定性。**注意：策划端已预置未跟踪新文件 `17_训练作战手册_陷阱图谱与灵活应对.md`（训练仓根），随本存档点 commit 一并入库**并在台账记一行「策划端 09-07 预置」。
- ⬜ **P0-2 通读序（顺序固定，每份读完在本条下打勾）**：HH.94 任务书 → AGENTS.md → 00 v1 → 15_账本（重点：一·补六/§六/§七）→ 02 边界 → 04 规格 → 07 启动指南 → 16 收手规则 → **17 作战手册** → 本清单。
- ⬜ **P0-3 指挥链声明**：TraeCode=主指挥（读战报/定位病根/定方案/亲跑验收/记台账），opencode=施工手（写码/改配/跑批）。每个 opencode 任务按手册 §1 四件套下达（目标/锚点/验收命令/禁区）；**opencode 一切「已完成/已通过」申报只作线索，验收=主指挥亲跑**（教训库 L-02）。在 HH.95 写明本批分工与 opencode 任务清单。
- ⬜ **P0-4 环境自检三件**：`dotnet build harness -warnaserror` 0 警告 0 错误（贴输出）；determinism 抽 1 场景同 seed 跑两次逐字节一致；`pwsh --version` 确认 7.x（禁 powershell.exe 5.1——PS5.1 无 BOM 中文 Select-String 失配前科）。

## 二、排雷图 M1~M16（开工必读；触雷处置=停下→定性→按处方→记台账，不赶工）

| # | 雷 | 处方 |
|---|---|---|
| M1 | **坐标消费面隐藏清单**：2D 化漏一处=该子系统静默停留 1D（编译照样绿） | **先列后改**：动手前 grep 全部坐标消费点产出清单（≥12 处预期：SimWorld/SimFormation/SimBrain/SimPerception/SimTask/CardPool/ScenarioGenV2/SimGroundEffectManager/escape_eval 场景/SimMetrics slotDev/场景 JSON/生成器锚点），逐处标注归属批次，清单入 HH.95 附录 |
| M2 | **SimTask 场景字段显式 1D**：15_账本 §7.8 记载 tasks 字段=workerId/type/**sourceX/destX**/workDuration——任务系统坐标是显式单轴 | tasks 字段 2D 化进 T2 清单；旧 1D 场景 tasks 单值 x→(x,0) 兼容解析；T-R/T-K 卡场景构造同步改 |
| M3 | **professions 字段三方错配**：crewRequired 事件先例（Unity 缺字段 JSON 静默 0）反向同样成立 | A2 落地先产出「字段映射表」：Unity asset 字段 ↔ professions.json 字段 ↔ ProfessionSnapshot 字段，缺源字段标 NOT_SET 并登记 15_账本，**禁静默填 0** |
| M4 | **SimChampion.Load 清空陷阱**：champion 缺职业→场景引用拿 default 快照静默（骑兵清空前例） | A2 收尾硬断言「**职业计数三相等**」：professions.json 职业数 == champion 注册数 == 全场景引用集合 ⊆ champion 集合；断言输出贴 HH.95 |
| M5 | **旧 1D 场景职业 id 失效**：professions v2 后旧场景引用旧职业 id（Undead_Warrior 等）→ default 快照或解析失败 | 决策：旧场景已挂起（C1 口径），**缺失直接 FAIL 快速暴露，禁 alias 静默改写**（静默 alias=教训 #5 同族）；解析兼容只保留 "Undead"→Monster 阵营字符串层 |
| M6 | **2D 新随机消费破确定性**：y 坐标/菱形散布/逃逸圈采样若用 Random | 全部走 SimRng 且 seed 注入链完整；determinism 必须跑 2D 新场景（不能只跑旧场景） |
| M7 | **微格/宏格混域**：Unity 寻路1 bug 的 sim 侧同款（微格域 1024² vs 宏格域 256²，divisor=4）；WildnessConfig 半径=格单位（D477 禁微格域） | SimGrid 双层 API 命名强制 Macro/Micro 后缀；坐标消费点逐处标注域别；**野性半径/感知半径按宏格** |
| M8 | **旧 18 卡冒烟跑错场景**：2D 化后 1D 场景 y=0 全挤一条线，跑了也是假数据 | 旧 18 卡冒烟一律用**新生成的 2D 卡池场景**；挂起的 1D JSON 禁跑禁改 |
| M9 | **holdout_v9 失去独立性**：与训练场景同 seed 池同分布=holdout 名存实亡 | holdout_v9 用独立 seed 段+人工审构成（覆盖矩阵命中记录） |
| M10 | **2D 性能坑**：1024² 微格全图遍历/感知 O(n²)→单局成本爆炸（v8 100 局超时先例） | SimGrid 禁全图遍历（感知走局部查询/空间索引）；**C1 先跑 5 局实测单局秒数再定 baseline 局数**（100 或 probe 30），实测数据入 HH.95 |
| M11 | **professions 单位错配**：2_20.1 数值直抄≠语义等价（Unity 格单位/cd 口径 vs sim 旧表 1D 调校值） | 对 3 个代表职业（战士/狼骑/弩炮）做 sim↔Unity 单位对拍记录；发现系统性偏差列报策划端，**禁静默缩放** |
| M12 | **Random 残留**：钩子六+生成器 2D+逃逸采样任何 `new Random()/Math.random` 残留都破确定性 | 批B 收尾硬项：grep `new Random|Random\.shared|Math\.random`（sim 侧惯例清单接单时按 Program.cs 现状校准）零残留输出贴报告 |
| M13 | **00 v2 与 AGENTS.md 文字冲突**：AGENTS.md「18 张任务卡/T01→T07 序」字样在 00 v2 后过时，但 AGENTS.md 本体禁改 | 00 v2 开头加「拓扑取代声明」：AGENTS.md 中卡数/卡序字样一律按 00 v2 拓扑执行，**AGENTS.md 铁律 0~9 纪律本身不变** |
| M14 | **opencode 申报不可信**（L-02 延伸到指挥链） | 见 P0-3；每个 T 任务的验收命令由主指挥亲跑，opencode 输出仅作输入线索 |
| M15 | **2D 布阵不对称**：等轴菱形两军不镜像→先手/地形偏差污染训练信号 | 生成器默认中心镜像对称布阵；非对称场景必须显式标注（EnemyAdvantage 语义沿用） |
| M16 | **一·补六清偿后状态不回填**：未来会话重做已清偿项 | 批C C3 逐条 #1~#8 过 checklist 回填状态（#1~#4/#6 本批清偿，#5 待 sim 多王国化注记，#7/#8 按 K 线口径） |

## 三、任务表 T0~T21（串行为主；每条：验收判据=最小完成定义）

| # | 任务 | 验收判据（最小完成定义） | 状态 |
|---|---|---|---|
| T0 | 开工前置 P0-1~4 | 四条全 ✅ | ⬜ |
| T1 | 00 v2 总纲落地 | 按任务书 §三转录+9 章节齐全（含 M13 拓扑取代声明+17 手册入阅读链）+旧 00 移归档（内容不动）+卡拓扑序草案列报 | ⬜ |
| T2 | 坐标消费面全量清单（A1 前置） | 清单产出≥M1 预期处数，逐处标域别（宏/微）与归属批次 | ⬜ |
| T3 | SimGrid 双层 2D 化 | 双层 API（Macro/Micro 命名）+微格 1024²/宏格 256² 契约+对角移动微格化 | ⬜ |
| T4 | 消费面逐处适配 | 按 T2 清单逐条改+逐条打勾（含 M2 tasks 字段） | ⬜ |
| T5 | professions 三方字段映射表 | 映射表产出（M3），NOT_SET 项登记 15_账本 | ⬜ |
| T6 | professions v2 数据落地 | 四族 46+28~37+机器+坐骑全量；数据源=2_20.1+附录A+资产 YAML 抽查≥10（M11 三职业对拍记录） | ⬜ |
| T7 | champion 受控重注册 | M4 三相等断言输出+**旧 tuning 值 diff=0 证明**（breakThreshold=3.0 等 3 组原样） | ⬜ |
| T8 | 种族/野性/自卫层 | raceId 入链+D468 三分支+D498 wildBase+D500 受击溯源自卫（不中断焦点） | ⬜ |
| T9 | 批A 门禁+野性四探针 | build 0w0e+determinism（2D 新场景）+四探针（异族必攻/同族结伙不攻/有国不主动/被击还手且焦点保持） | ⬜ |
| T10 | 感知 2D 化 | 欧氏圆+单遍过滤（D509 口径）+1D 轴距离残留清零（grep 佐证） | ⬜ |
| T11 | 逃逸完整链 | 圈采样×扇区加权×避敌×IsWalkable+directionSectorWeight 入 factor_registry | ⬜ |
| T12 | 移动/冲锋 2D | MoveTowards 2D+ChargeSweep 冲锋几何 | ⬜ |
| T13 | 吸引场+意图权重 | 机制落地+对照开关（useTable/usePins）可切换 | ⬜ |
| T14 | 新兵种钩子 sim 化 | 六钩子（狂战/磐石/盾卫庇护/火枪/对建筑×2/骑射）+M12 grep 零残留 | ⬜ |
| T15 | 场景生成器 2D | 镜像对称（M15）+地形维度（窄口/水域/山体/城墙）+四族+28~37 全入池+tasks 2D 化 | ⬜ |
| T16 | 旧 18 卡 2D 冒烟 | 各卡≥3 个新 2D 场景跑通（可跑+无 NaN/卡死/越界；M8 禁跑 1D JSON） | ⬜ |
| T17 | v9 分代建档+成本实测 | M10 五局实测数据+baseline 局数定案+suite_v9 产出+旧 v8 挂起标注 | ⬜ |
| T18 | holdout_v9+门禁全跑 | M9 独立 seed+build/determinism/baseline v9/holdout 四件全绿输出 | ⬜ |
| T19 | 15_账本全量登记+回填 | 行为/参数/语义变更全登记+Unity 适配义务新条目+M16 逐条回填 | ⬜ |
| T20 | 卡骨架 46 张落盘 | F 31（含每卡「正确行为收益可见性」自查——手册罪1）+K 15 定义占位；旧卡台账不删 | ⬜ |
| T21 | HH.95 完成报告 | 含清单回执区逐条状态+M 雷处置记录+门禁输出+影响面清单附录 | ⬜ |

## 四、坑位级施工详规（关键三处）

**详规1 SimGrid 双层**：API 命名 `IsWalkableMacro/IsWalkableMicro`（禁无后缀重载）；微格=宏格÷4；感知半径换算集中一个函数（禁散落换算系数）；感知查询走局部/空间索引禁全图遍历；wild 半径宏格（D477）。
**详规2 professions v2 落地流**：真源序=2_20.1 正文+附录A → Unity 资产 YAML 抽查（≥10 职业）→ 冲突列报；映射表逐字段（M3）；落地后三相等断言（M4）+三职业单位对拍（M11）；champion 重注册前 git 存档点、tuning 段 diff=0 验证（T7）。
**详规3 v9 建档流**：生成→去重→覆盖矩阵校验→holdout 独立 seed 段（M9）→单局成本实测（M10）→baseline 建档→旧 v8/Holdout 原地挂起标注（禁改禁删）→分代口径写进 00 v2。

## 五、完整性校验表（交 HH.95 前逐项打勾）

- ⬜ 门禁四件输出在场（build 0w0e/determinism 2D 场景/baseline v9/holdout_v9）
- ⬜ M1~M16 逐条处置记录在场（未触雷也记「确认未触发」）
- ⬜ 一·补六 #1~#8 状态逐条回填
- ⬜ 职业计数三相等断言输出在场
- ⬜ Random grep 零残留输出在场（M12）
- ⬜ 旧 tuning 值 diff=0 证明在场（T7）
- ⬜ T2 影响面清单在场且逐条有状态
- ⬜ 卡骨架 46 张在场（31 F+15 K）
- ⬜ 15_账本 Unity 适配义务新条目齐全
- ⬜ 00 v2 含 M13 拓扑取代声明+17 手册阅读链
- ⬜ HH.95 回执区=本清单 T0~T21 逐条状态

## 六、排期建议

批0 半天 / 批A 1~2 天 / 批B 2~3 天 / 批C 1 天。M 雷触发即停即记（不赶工）；连续触雷 ≥3 条 → 停下按手册 §五元原则升级策划端，勿边排雷边硬闯。
