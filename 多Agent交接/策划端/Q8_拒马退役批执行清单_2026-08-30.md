# Q8 拒马退役批执行清单：D122 落实（Unity 代码批 + 资产清理 + sim 停生成）

> 签发：策划端（2026-08-30，D122「拒马砍掉」裁决落实立项，用户拍板「立项吧」）。
> **背景**：D122（0.6 §十）终裁砍除拒马，但仅 2_12 建筑清单行回写，实现层全链路在役（职业/资产/prefab/减速逻辑/Debug 入口/sim 生成器/4 个 R 共享场景）。本批=全链路退役。
> **锚点**：主仓 @ Q8 签发 commit（执行前 `git log -1` 实测为准）。
> **总纪律**：本批允许改动面 = T2/T3 点名的 Unity 代码与资产 + T4 训练仓 harness Sim 层 + HH 交付文件。**设计文档零改动**（文档批策划端已收：2_12×3 + 2_3×3，2026-08-30）。红即停、报策划、不自修、不虚报（HH.37 铁律）。
> **衔接**：阻塞于 Q7 清债批完成（git/编译门互斥）。本批完成 → 转《Q1_步骤12全量收官回归执行清单_2026-08-29.md》从其 T2 编译门开始。交付报告独立 **HH.40**（§一=Unity 批、§二=sim 批、§三=探针与门禁数据）；Q7+Q1 仍合并 HH.39 不变。

---

## §1 任务总表

| 编号 | 任务 | 类型 | 依赖 | 出处 | 验收标准（验收句） | 证据 |
|------|------|------|------|------|-------------------|------|
| T1 | **git 环境预检**：实测 `git status`/`git log -1` 正常；训练仓 `ai决策大脑强化训练/` 独立 git 状态同样预检 | 流程 | Q7 完成 | HH.37 R5 教训 | 两仓 status 输出正常、无滞后报错 | `git status --short` 两仓输出截录 |
| T2 | **Unity 资产清理**：①删 4 份资产（连同 .meta）：`Resources/UnitData/Barricade.asset`、`Resources/Buildings/Barricade.asset`、`Resources/Fortifications/Barricade.asset`、`Resources/UnitPrefabs/Human_Player_Barricade.prefab`；②`Resources/Modules/Module_Civil.asset` 删 `unlockBuildings:` 下 `- Barricade` 行（约 L32）；③删前先 grep 该资产在全工程的其他引用面，发现清单外引用→停，报策划重裁 | 资产 | T1 | Q8 立项调查（2026-08-30） | 4 资产+meta 已删；Module_Civil diff 仅 1 行删；Unity 启动无 Barricade 资产加载报错 | asset diff + 删前 grep 输出 |
| T3 | **Unity 代码清理**：①`UnitController.cs` 删 `ApplyBarricadeSlowIfNeeded()` 及 2 处调用（L1139/L1167 附近）+相关注释；②`FortificationDef.cs` 删 `barricadeSlowFactor`/`barricadeSlowDuration` 字段及拒马 header 注释（blocksMovement/heightCells 注释里的拒马字样一并清）；③`AIDebugSpawnController.cs` 删 `DebugSpawnType.PlayerBarricade` case+显示名+菜单项（枚举值处置按 §3 预授权）；④`PlaceholderSprites.cs` 删 `bld_wall_barr` 行；⑤注释清理：`NPCBrain.cs`（L1398 附近冲锋注释）、`FormationController.cs`（L225 附近）、`ProfessionSnapshot.cs`（Unity 侧工事注释）、`UnitData.cs` Occupation.Barricade 行尾注释改「D122 砍除退役，值保留防序列化错位（Q8）」；⑥**全库 grep 复核**：`git grep -in barricade -- "Valley Rampart/Assets"` 期待仅剩 Occupation 枚举值行+注释级残留，活逻辑/活引用零命中；`UnitDataManager` Barricade key 硬编码如有→一并删 | 代码 | T2 | Q8 立项调查 | 编译 0 error 0 warning；grep 复核输出=仅枚举值+注释；Play 无异常 | 各文件 diff + grep 输出 + 编译截录 |
| T4 | **sim 批（训练仓，先 activate sim-sync）**：①`Sim/ScenarioGenerator.cs`、`Sim/ScenarioGenV2.cs`、`Sim/CardPool.cs` 停新生成 Barricade；②**解析兼容保留**：`Sim/SimWorld.cs`/`SimUnit.cs`/`SimBrain.cs`/`SimConfig.cs`/`Program.cs` 对 Barricade 的读取/解析分支一律不动（4 个 R*.json 共享场景含 Barricade，必须继续可跑）；③`Core/Config/ProfessionSnapshot.cs` 拒马注释清（通用工事字段保留不动）；④**双门禁**：`dotnet build` 0 警告 0 错误 + baseline 对比（无场景退化 >5%）+ holdout 不退 + 同 seed 确定性；⑤`15_训练侧harness与Unity端差距文档.md` 登记一行（Unity 拒马已退役 vs sim 解析兼容保留，双向口径+Q8 指针）；⑥训练仓纪律：改前 commit 当前态 → 改 → 双门禁 → 通过后 commit，失败回滚+记台账 | sim | T1 | Q8 立项调查 · sim-sync · CODELY.md §七6 | 门禁全绿；账本 diff 含新行；共享场景 4 个 R 原样未动 | build/benchmark/holdout 输出 + 训练仓两 commit + 账本 diff |
| T5 | **收尾**：HH.40 交付报告落 `多Agent交接/执行端/` + `_交接索引.md` 登记 HH.40 行 + git-plan-sync 工作日志插行；主仓与训练仓各自 commit（**add 共享文件前先看该文件完整 git diff**，防吸收并行在途改动——cbca4980 教训）；报策划验收 | 流程 | T2、T3、T4 | vr-triage-flow §九 · HH.23/37 教训 | 索引含 HH.40；commit 构成与报告逐文件一致 | HH.40 文件 + 两仓 `git show --stat HEAD` |

**行为级探针（T2/T3 验收必含，结构性 grep 不作为通过依据）**：
1. 负探针：Debug 生成入口无拒马项；建造菜单（Civil 模块）无拒马条目。
2. 正探针：城墙生成→阻挡生效；城门→通行/开合正常；箭塔→索敌攻击正常（工事兄弟功能零回归）。
3. 加载面：Play 真开局，console 无 Barricade/拒马相关报错、无模块解锁死引用。

## §2 预授权与边界裁决

1. **`Occupation.Barricade` 枚举值必须保留**（L26 中间位，后方 Wall/Gate/三塔/Resident/Worker 等按 int 序列化，删除即全量 .asset 错位——项目「末尾追加保 int 稳定」铁律）。只做引用面退役，枚举行加废弃注释。
2. `DebugSpawnType.PlayerBarricade`：grep 确认无序列化/反射/int 转换引用→可删枚举值；有→保留值仅删 case 与菜单项。HH.40 写明所择路径与依据。
3. sim 侧**禁 gen-r 重生成共享套件**、禁手改 R*.json（4 个含 Barricade 场景=解析兼容的活测试用例，原样保留）。
4. sim 停生成后新 gen 场景不含拒马=预期行为，不算与 Unity 差距；存量场景含拒马仍可跑=预期兼容，均无需登记差距（账本只登记 §1 T4⑤ 一行口径）。
5. champion/tuning、Holdout、AGENTS.md、factor_registry 禁改（训练边界铁律）。

## §3 禁改清单

1. 设计文档（河谷防线开发计划书具体内容/**）零改动——含美术三件套与 QQQ.5 附录A（残留台账见附录A，策划端随批收尾，执行端勿动）。
2. 共享场景 `harness/Scenarios/R*.json`（主仓根）与训练仓 `harness/Scenarios/econ_*`。
3. `Occupation` 枚举值序列（只允许加注释）。
4. 队列只读；状态由策划端验收后转移。

## §4 停机判据

- T1 任一仓 git 异常 → 全批暂停先报环境。
- T2 删前 grep 发现清单外引用面 → 停，保留现场报策划重裁。
- T3 编译错 / T4 门禁退化或确定性破 → 停，归因按「让渡三问法」推到真帧同条件再下结论，不自修产品码。

---

## 附录A：文档残留台账（策划端处理，执行端只读）

**随 Q8 代码批收尾由策划端一次对齐**（时点=T2/T3 资产与素材行删除落地后，防文档先行漂移）：
- `改造计划/美术资源规范_等轴立方体瓦片.md` L103（bld_wall_barr 行）/L110（1×1 用途句）
- `3.1.1_美术资源清单.md` L45/L155（§2.6 拒马阵章节）/L623/L734（土木清单句）
- `3.1.2_NPC美术资源清单.md` L620（§4.6 章节）/L795（✅ 生成记录行）
- `QQQ.5_附录A_可生产职业全集初稿表.md` L23（职业 8 格划线）
- `CODELY.md` L86 回灌验收句「/ 拒马减速 /」→ **用户手动移除**（CODELY.md 为记忆文件，工具禁直改；已提醒）

**免改记录**（防未来对账重复立案）：
- `3.5_王国经营体系.md`×8 / `3.5.4_建筑数据卡.md`×5：被 2_12 接管的 1D 旧文档，拒马行随真源（2_12 建筑清单+Module 资产）失效，不逐行回写。
- `3.6_军事统一管理.md` L172/L193：L3 已有迁移标注（「拒马已砍 D122」），数据源细节以 2_5/2_12 为准。
- `3.7_军事战斗完整落地与训练接入.md`×13：P1.4/P1.5 拒马减速实施记录=历史修复日志，不改写（doc-management 历史行纪律）。
- `实施计划_军事与王国体系.md` L37：旧实施记录。
- 训练仓 `Core/Config/ProfessionSnapshot.cs` 通用工事字段注释级拒马字样：T4③ 清理。
