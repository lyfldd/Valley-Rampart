# HH.153 AINPC 弹话文案族化批 · 任务书

> 类型：施工任务书（**策划端 → 执行端**）
> 状态：🟡 已签发待接单（2026-09-10）
> 日期：2026-09-10 · 发起端：策划端
> 用户裁决（2026-09-10）：AI 弹话文案「丰富+搞怪+每族每职业不同」方案与全稿拍板通过 → 派活执行端实施
> 纪律钩子：vr-id-ledger（HH.153/154 已登记账本）· vr-planner-leadership 钩子1（教训引用见 §四）· 并发文件冲突纪律（改前必重读+查 untracked，D533 事故教训）
> 完成报告：HH.154（同笔预留）

---

## 〇、接手入口（执行端开工恢复序）

1. 读 `河谷防线开发计划书具体内容/QQQ.6_AI弹话文案族化与丰富化.md`（**设计真源，文案全稿在此，逐字录入勿自行改写**）+ `QQQ.6_执行清单.md`（任务表 T1~T6）
2. 读本任务书全文 → 按 §五 纪律开工
3. 当前局势快照（2026-09-10）：Unity 轨批E（HH.148）🟡已签发待接单、测试基建（HH.150）🟡批E 后；本批与二者**零文件面冲突**（只动 UnitController.cs + AttentionTuningConfig），可并行接单

---

## 一、背景

用户拍板（2026-09-10）：AINPC 时不时弹出的头顶话要**更丰富、更搞怪**（搞怪参考选族文案 HH.96 §四 抽象搞怪版），**每一族的话不一样、每一职业不一样**。性能之前已考虑（QQQ.2 DR-10 全套管控），本批零新增运行时开销。

现状核心缺口：`UnitController.GetTalkPool`（17 职业 × 三态）**无种族维度**，兽人战士与人类战士说一模一样的话；且 9 个战斗职业共用"随时准备战斗！"开场，辨识度低、不够搞怪。

---

## 二、范围（件 = QQQ.6 清单任务）

| 件 | 任务 | 说明 |
|----|------|------|
| 件1 | QQQ.6 T1 | AttentionTuningConfig 加 `talkRaceChance`（0.4）SO 化；**不进 ToSnapshot()**（表现层，sim 零义务） |
| 件2 | QQQ.6 T2 | `PickTalkLine` 读自身 `raceId` 双层混合（正常态 40% 族池 / 60% 职业池；饥饿/受伤态 100% 职业状态池） |
| 件3 | QQQ.6 T3 | 新增 `GetRaceTalkPool(raceId)`：4 族 × 8 句逐字录入（QQQ.6 §需求2） |
| 件4 | QQQ.6 T4 | `GetTalkPool` 十七职业文案全量替换（QQQ.6 §需求3 定稿）；**战斗职业删"随时准备战斗！"共用开场** |
| 件5 | QQQ.6 T5 | 对话池提 `static readonly`（禁每次 new string[]） |
| 件6 | QQQ.6 T6 | 进局验证（正门 EnterTestRun）+ AI NPC raceId 非全兜底 Human 抽查 |

**红线**：文案逐字录入 QQQ.6 定稿，禁止执行端自行润色改写（设计归属策划端，有异议列报请裁，不擅改）。

---

## 三、实施清单

以 `QQQ.6_执行清单.md` 为唯一执行载体（T1~T6 + 依赖 + 完整性校验已全）。本任务书不复述，防漂移。

---

## 四、排雷图（教训引用，签发前已读教训库）

| 编号 | 教训 | 本批处置条款 |
|------|------|------------|
| M1 | **L-01 新增内容消费端断链**（策划端必答：AI 消费端是否就位） | 族维度消费端=`raceId` 分流。件6 硬性抽查：**AI 王国 NPC raceId 非全兜底 Human**（D467/Q10-M2 已回填 2026-09-03，理论可用；若实测大量兜底 Human → 列报，勿静默） |
| M2 | **L-17 容器入口纪律**（凡进局测试必走正门） | 件6 进局验证一律 `TestHarnessApi.EnterTestRun`（判负守卫全开）+ `ExitTestRun` 收尾；禁 SmokeApi.EnterGame 裸跑 |
| M3 | **L-08 门槛遮蔽**（底数不足观察不到行为=误判未做） | 弹话触发依赖 `IsIdleForTask + SafetyScore>0.6`。验证观察窗须置于安全环境（城墙内/无威胁），否则 NPC 不弹=环境问题非实现缺失 |
| M4 | **L-14 探针判据环境噪声** | 弹话为概率随机句，断言"族池句出现"用**多句采样取存在性证据**（累计 N 次随机命中族池句），禁单次断言 |
| M5 | **L-21 目标驱动循环计数面**（本批弱相关但通用） | 弹话冷却/上限机制 QQQ.2 DR-10 已封死，本批**不得触碰** OverheadSpeechManager/NPCBrain.TickAutoTalk 触发面；只换文案口子 |

**教训核查结论**：本批相关引用 M1~M5；无未命中项。

---

## 五、探针与验收

- 探针清单：QQQ.6_执行清单 T6 + M1~M5 处置条款逐条留证
- 验收硬条款：
  1. grep `随时准备战斗` 在 UnitController.cs **零命中**
  2. grep `talkRaceChance` 在 cs+asset 双在场 + `ToSnapshot()` 无该字段（sim 零义务实证）
  3. 4×8 族池句 + 17 职业池与 QQQ.6 逐字比对零差异
  4. 编译 0 error 0 新警；弹话频率/上限/冷却行为无回归（与 QQQ.2 口径一致）
- 完成报告=HH.154（预留），报告需含：git 面构成 + grep 双锚点 + 探针判读 + 列报（含 AI raceId 抽查结论）

---

## 六、执行与回写纪律

1. 动文件前必重读磁盘最新内容（并发纪律）；同文件多处编辑串行执行（L-12）
2. 每件完成：落盘 → QQQ.6_执行清单 勾选 → 收口写 HH.154 完成报告
3. 语言中文；**不改设计文档**（QQQ.6 文案归策划端），遇文案异议列报请裁
4. commit + git-plan-sync 随执行端惯例（git-plan-sync 钩子）

---

## 七、回执区（执行端施工后填写）

- 件1：⬜ → 件2：⬜ → 件3：⬜ → 件4：⬜ → 件5：⬜ → 件6：⬜
- 探针判读：
- 列报（AI raceId 抽查 / 文案异议 / 超范围发现）：

---

> 签发：策划端 2026-09-10 · 文案全稿已用户拍板 · 接单即开工，完成报告落 HH.154
