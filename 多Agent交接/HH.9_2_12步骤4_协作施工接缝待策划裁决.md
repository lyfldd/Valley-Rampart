# HH.9 2_12 步骤4 协作施工——承接步骤3凑单的落地口径待策划裁决

> 类型：待决策（接缝落地口径）
> 状态：✅已裁决（2026-08-22 策划端，见文末回写）
> 日期：2026-08-22 · 发起端：执行端 · 关联：2_12 步骤4（实施计划 L101-119）/ 设计稿 §5.0 建造/升级流程（L232-240）/ D44/D112/D95/D161 / 步骤3 凑单（HH.8，已提交 `70d355b`+`fa8d876`）

## 一、现状（执行端核实的证据）
1. **现有建造流程 = 放置即扣全资源 + 纯计时施工**：
   - [BuildController.TryPlace](file:///c:/Users/trs/Desktop/Valley%20Rampart/Valley%20Rampart/Assets/_Game/Systems/Building/BuildController.cs) L204-205：放置时即 `WarehouseHelper.CanAfford + TrySettle`（**一次性扣全成本**，步骤3 刚落地）。
   - L232：`b.Init(...)/b.StartConstructing()`。
   - [Building.StartConstructing](file:///c:/Users/trs/Desktop/Valley%20Rampart/Valley%20Rampart/Assets/_Game/Systems/Building/Building.cs) L306-313 + Update L321-331：`constructDuration=5f` 纯计时推进，到 100% 自动完成。**无"工人搬运资源→目标建筑仓库积累"环节**。
2. **施工态核心已存在**：BuildingState.Constructing + constructProgress + 脚手架视觉 + 升级/修复复用（批次3 已有），步骤4 不是从零建状态机。
3. **2_8 已有搬运任务基础设施**：`Building.TryAdvertiseTask` ③ Transport 任务（storage≥threshold → 搬运到 NearestWarehouse）+ `ScaleTaskArgs{totalResourceDemand}` 规模派工（D95），`WorkerInventory` 移动仓库已实现 IWarehouse（步骤3）。工人搬运闭环（采集→背包→仓库）在 TaskScheduler 已跑通。

## 二、接缝冲突（需策划裁决的核心）
设计稿 §5.0（L232-240）要求**协作施工流程**：
```
放置目标 → 建筑发布任务（资源总需求如500石，D95规模派工=理想工人数）→
工人去王国仓库取资源→搬运→填入目标建筑"仓库"→ 仓库积满 → 触发施工期（多工人协作）→ 完成激活
```
关键语义：**资源通过任务搬运逐步积累到目标建筑**，不是放置时一次性扣。

**而步骤3 我刚落地的是"放置时凑单一次性全扣"**（`WarehouseHelper.TrySettle`）。若按设计稿改，放置时不再全扣，改为"占位 + 广播搬运任务 + 目标 StorageComponent 积累 + 积满触发施工"。这会**推翻步骤3 刚提交的凑单落地**（建造点击从"结算扣减"变为"发任务"）。

## 三、待决策项（选项 + 推荐）
1. **建造资源收取方式**：
   - A（推荐）：**分两段**——放置时仍由 `WarehouseHelper.TrySettle` 从王国仓库扣**总成本**，但扣费后的"施工"改为**目标建筑 StorageComponent 预存入该成本资源**（模拟"仓库积满"起点）→ 施工期=多工人协作推进（constructDuration 按协作人数缩短，复用现有 Constructing 态）。改动最小：不动步骤3 凑单、只给 Building 加"协作施工计时按工人数缩放"。工人协作人数从哪来（2_8 派工或固定）需定。
   - B：严格按设计稿——放置只发布搬运任务，资源经工人实际搬运逐步积累到目标建筑仓库，积满才触发施工。改动最大：重写放置流程 + 新增目标建筑仓库积累逻辑 + 与 2_8 搬运任务深度耦合，且**需推翻步骤3 凑单落地**。
   - C：本步只落"多工人协作施工"（施工耗时随协作工人数缩短），资源收取保持现有（放置即扣），"搬运积累"整个留后续步骤。
2. **协作施工的人数来源【若选 A/C】**
   - `TaskScheduler` 已派工给该建筑的工人数（`HasWorkerAssigned`），或 2_8 `ScaleTaskArgs` 规模派工给出的理想工人数。
   - 施工耗时 = `constructDuration / 协作加成`（加成 = 1 + (工人数-1)×系数，系数 SO 可配）。
3. **验收路径**：步骤4 验收（L119）"放采石场→派工人→产石→搬运入就近仓库（2D）闭环；建造/升级=仓库积满→协作施工跑通"。若选 A，验收口径需缩围为"建造/升级=扣费→协作施工跑通"（资源收取=凑单已验）。

## 四、我的倾向
- 回 HH.8 已把步骤3 敲定为"凑单自结算时点"，因此**建造收费已走仓库凑单**。为不推翻既有落地，倾向 **A（最小改动）：分两段——放置扣费（已好）+ 协作施工计时按工人数缩放**。资源"搬运积累"若策划坚持设计稿原文，则需接受推翻步骤3 凑单、改 B。
- 关键待策划定：**协作施工语义**（要"真实搬运积累"还是"扣费+计时缩放"）+ 协作人数来源 + constructDuration 是否需 SO 化（当前 5f 硬编码，note 到步骤）。

## 五、下一步建议
- 策划裁决落地口径（A 最小 / B 严格设计稿）+ 协作人数来源 + constructDuration SO 化归属后，执行端按序开工步骤4。
- 恢复入口：本 HH + 2_12 实施计划步骤4 + 工作日志。

---

## 策划裁决（策划端回写，裁决前保持空白）

| 决策点 | 裁决 | 理由 |
|--------|------|------|
| 1. 建造资源收取方式 | **C+（选给定 C 升级）：协作缩放 + 目标仓库虚挂账全免**。**A 否决**——"预存"是给系统撒谎：账面显示目标仓库有 500 石（Query 可读）但为幻影，AI 决策核（2_8 派工/2_9 对拍）读到假数据，违背 D43"资源不凭空位移"本意；**B 否决**——真实搬运是设计稿本意但当下推翻步骤3 凑单+深耦合半成品通道，且步骤4 流程第1步"玩家指定目标"归 2_13，验收舞台未成；**C+**=凑单收取（已验）+协作缩放落地+目标建筑不挂任何假仓库。D161 退化注记："施工中断可继续=进度保留；资源留施工仓库语义待 B 语义补全（债务卡）" | AI 北极星（假账污染决策输入）> 设计稿忠实度 > 最小改动 |
| 2. 协作人数来源 | **n=该建筑当前实际被派工人数**（TaskScheduler HasWorkerAssigned 计数）；**不虚增"理想工人数"**——ScaleTaskArgs.IdealWorkers 是 B 语义配套（需求/携带量），C+ 期用实际数。公式 constructDuration/(1+(n-1)×k)，**k 进 SO** | 理想工人数在无搬运语义下无意义，虚增=又一个假数据源 |
| 3. constructDuration SO 化 | **本步顺手做**：5f 硬编码违 SO 铁律，且本步就要动它（除协作加成），一次改完（归 BuildingConfig 或施工相关 SO） | SO 铁律 + 改动点重合 |
| 4. 验收路径 | **缩围确认**："建造/升级=扣费→协作施工跑通（缩放+中断续造）"+采石场搬运闭环复跑；RulerController/凑单部分不重验 | 与 HH.8 验收缩围口径一致 |

### 分歧裁决记录（有分歧时必填）
- 执行端意见：倾向 A（最小改动，预存模拟仓库起点）。
- 策划端意见：破框否决 A——A/B/C 共享前提"资源收取只有两种位置"未被检视，A 的预存=记账魔术绕设计（Query 假账污染 AI 决策输入）；C+ 保持账面诚实，B 语义挂债务卡不废弃。
- 裁决：C+ · 依据：AI 北极星（一切为了 NPC AI，决策核读假账=对拍根本失效）/ 三支柱（资源不凭空位移 D43）/ 单人规模（B 的完整舞台在 2_13 后，现在做=半成品耦合）

### 衍生产物
- 新建清单/文档：**B 语义债务卡**：设计稿 §5.0"搬运积累→积满触发施工"未废弃，归还时机=2_13 交互入口落地后（届时推翻的仅"放置扣费"段，凑单算法保留复用）；登记入开发计划书待办/下张 HH