# HH.4 落A 时序（doc 契约修订收官）——交验收分类表 + 指向新会话代码侧

> 类型：进度同步 + 待决策（验收确认）
> 状态：⏳待裁决
> 日期：2026-08-22 · 发起端：执行端 · 关联：HH.3（此前裁决 A：统一 iso 基准 / 三纪律）、doc1、1_2D、2_10
> 说明：承接 HH.3 裁决 B——本会话**只做纪律1"doc 契约修订"并提交，代码零改动**。本 HH 把验收交给策划确认，并把下一步（纪律2/3 代码侧 + 四链取证）交接给新会话。

## 一、做了什么（执行端填，带证据）

裁决 B 收官，提交 **`89cb73c`**（14 文件 +36/−24，**仅文档**）：

- **doc 契约修订（纪律1）**：措辞统一为"逻辑=格基准、世界=等轴嵌入，`CoordToWorld`=渲染 `GridToIso` 同一条 iso 映射"，每处带 `2026-08-22 裁决（HH.3）` 迁移标记。涉及：1_2D/doc1_实施/2_10 设计·实施/2_10_实施计划/0.5 路线图/2_1实施/2_2/2_3(+实施)/2_5实施/2_8实施/美术规范。
- **HH.3 §四补行**：环境说明后插入 `SetupStableCamera(true)` 相机锁定前置约定（随 ee5be68 相机闭环）。
- **开发计划书**：新增 08-22（待提交）日志行，下一步归集到"落A 代码侧原子改"。
- 四边界全遵守：①历史台账不改 ②`GridConfig.cs` 注释不动 ③迁移标记齐全 ④验收=grep 分类表（见下）。

## 二、现状与阻塞

- 纪律1 完成。纪律2（全调用方回归+全象限探针）、纪律3（移动速度 SO 重标定）与四链取证（①采集搬运/④守卫迎战/⑥巡逻探雾/K7 撤退回位）**未做**——需在**新会话**跑。
- 阻塞点已由 HH.3 裁决：坐标系基准统一后，`BuildingPlacedEvent` 无订阅者洪水、相机边缘滚动等均已闭环。

## 三、待决策事项

### 决策1：请策划验收「正交」grep 分类表（裁决④验收口径）
本会话遗漏"正交"归属三类，残留是否可接受需策划确认（选项+推荐+影响）：

- **A（推荐）**：采纳分类验收——残留 正交 均为 {①契约已带迁移标记} / {②历史台账} / {③非契约几何语义}，契约性残留仅有 1_2D L6/L312/L527、2_8实施 L85 且均已标注"代码仍正交"留待纪律2 一并落 iso。**影响**：验收不看"零残留"而看"分类归属准确"，纪律2 另行消灭契约性残留。
- **B**：要求全部契约性残留也加迁移标记后再交。**影响**：多一轮 doc 编辑；因代码仍正交，标记颗粒度与纪律2 原子真改可能纠结。

### 决策2：确认下一个会话直接做「落A 代码侧原子改」？
- **A（推荐）**：新会话按 HH.3 七的入口执行：`GridSystem.CoordToWorld/WorldToCoord` 改 iso → 全调用方回归 → 全象限 round-trip 探针 → 移动速度 SO 按 0.716 格重标定 → `BuildingPlacedEvent` 订阅收窄 → 重建单位铺放 → 四链 grep 取证清 2_8 尾巴。**影响**：一步到位完成纪律2/3+取证。
- **B**：先只做坐标系原子改与回归探针，四链取证单独排。**影响**：更小的单会话范围，But 取证延迟到下下会话。

## 四、下一步建议（恢复入口）

1. 策划回写本 HH 裁决 → 状态改 ✅ → 更新 `_交接索引.md`。
2. 新会话读 `河谷防线_开发计划书.md` 顶部 08-22 日志行 → 读本 HH + HH.3 裁决 → 按 HH.3 七入口执行落A 代码侧。

---

## 策划裁决（策划端回写，裁决前保持空白）

| 决策点 | 裁决 | 理由 |
|--------|------|------|
| 决策1 正交grep分类验收 | 待裁决 | |
| 决策2 新会话做落A代码原子改 | 待裁决 | |

### 分歧裁决记录（有分歧时必填）
- 执行端意见：.. · 策划端意见：..
- 裁决：.. · 依据：AI 北极星 / 三支柱 / 单人规模 / 默认砍功能

### 衍生产物
- 新建设计文档：无
- 新建清单任务：落A 代码侧（纪律2/3 + 四链取证，见 HH.3 七）