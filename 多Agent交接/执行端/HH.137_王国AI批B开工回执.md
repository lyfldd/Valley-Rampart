# HH.137 王国AI P0 批B 开工回执（建军链 B1~B8）

> 日期：2026-09-09 · 持有端：执行端（TraeCode·Unity 轨）
> 上游：D592 批A 验收销号→批B 解锁 · 任务真源=2_22 P0 清单 §二批B
> 完成报告=HH.138（同笔预留）

## 一、编号报备

| 编号 | 用途 | 状态 |
|------|------|------|
| HH.137 | 本开工回执 | 🟡 占用 |
| HH.138 | 批B 完成报告 | 🔵 预留 |
| 取号说明 | 账本直读：130🔵（训练轨判读）/131🟡（零碎包）/132🟡（代码化二态）/133🟡（UI 小批）/134~136🔵（报告预留）全占 → 顺延取 137/138 | — |

## 二、批B 范围（2_22 §二，验收标准照录不改）

| 编号 | 任务 | 验收标准（清单原文） |
|------|------|---------------------|
| B1 | TrainingSystem 暴露系统级（非 UI）入口供 AI 调用（核对 CanTrainGeneral L536/generalLimit 既有链）——AI 与玩家同链 | 公共系统入口在场；AI 调用与玩家 UI 入口同一校验链（generalLimit 对 AI 生效） |
| B2 | ⑯训练将军行动（阶段可见性=军事期起 P2 迁处境门控）+NeedScore（缺将军）+执行=卫戍营队列训练 General | AI 焦点选中⑯→卫戍营队列→将军实体出现（行为级断言 §十.1 探针1） |
| B3 | ⑰建军事建筑行动+ExecuteBuildFocus 军事清单扩容——练兵场（共通）+战营/射箭场/战争学院（M6 在场）；**熔炉 LeyForge=经济向不配 F1**（§3.7 边界）；选型=种族+快照缺口 | AI 建造清单含军事建筑；选型探针（兽人候选含战营、熔炉不进军事向） |
| B4 | RaceDef 兵种出厂倾向先验（per 兵种倾向值）——D519 先验=种族底色+性格映射起步 | RaceDef 字段在场+四资产回填；S-3 登记行在 factor_registry 草案 |
| B5 | 局内环自整定 BattleLearnedWeights：战斗结算→表现统计→wᵢ←wᵢ×exp(η×(表现分−均值))，clamp 安全栏+多样性下限；同 seed 完全复现；归因日志 | 同 seed 双跑权重序列逐字节一致；单兵种超安全栏被 clamp（负探针）；归因日志在场 |
| B6 | ⑦招兵扩多兵种：招募分=出厂倾向×性格调制×表现统计×经济可负担÷多样性惩罚；走 TrainingSystem 同链；**非 Warrior 直转**；可训域过滤（D570：共通+本族）；建筑前置联动（缺建筑兵种本轮不可选招） | 同 seed 选招序列确定（探针3）；改先验→分布变化（正探针）；可训域负探针（异族专属权重恒 0） |
| B7 | 将军成军链+补任：在场自动 BindGeneral→RecruitStandard 招兵成军；补任=A6 事件→快照更新→缺将军可评分→⑯再入选 | 将军在场编队成员>0；将军阵亡→数日内再训练新将军 |
| B8 | 战争机器双行动（D558→D570）：㉔建 SiegeWorkshop+㉕造机器（NeedScore=战争态势驱动[军事期+攻城/守城需求]）+`ProduceMachine(type,spawnPos,kingdomId)` 同链；族门禁 IsRaceAllowedMachine+上限守卫（2+每级2）；**不进配兵双环**；人类 Ballista prefab 在场可行为级验证；sim 义务=15_账本登记（E2 同串）；**注释义务=SiegProductionSystem L176 过时注释更新（DZ-083c）** | 族门禁负探针（异族机器拒）+上限负探针（超 2 拒）+正探针（军事期战争态势→机器产出+军力现状口径含机器） |

## 三、D590 增补节+D592 前置注记落法（内源节拍完整落地归本批）

1. **内源势能项结构**（增补节2）：Expand/军事期军事行动 NeedScore += 内源势能项=经济盈余率/人口压力/仓储水位三输入的连续函数（线性加权起步=最简连续函数）；袭扰/威胁=叠加项不删（⑦既有 D348 通道保留）。
2. **数据源**：快照军事块尾增设"内源势能输入"三字段（BuildSituation 从 KingdomState 现算填充——R-A1 经济诊断块落地后由真值块接替，字段注记交接关系）。
3. **权重 SO 化**（增补节2/3）：SituationConfig 增三参数（内源势能权重，初始 **0.1=保守量级下沿**，禁调值；可训练标量预留=factor_registry S 行登记）。
4. **行为边界**（增补节4）：只加评分项，ScriptStageMachine 阶段机结构零触碰。
5. **负探针**（增补节5/D592）：内源势能权重置 0 → 军事行动评分退化（死滞复现自证）随批B 探针容器。
6. **P4a 口径承接**（D592）：批A P4a 的 1.00=缺口存在度；批B 行动条目落 NeedScore 权重合成后，缺口项与内源项权重各自独立可辨。

## 四、锚点直读状态

- 已实读：KingdomBrain/UtilityScorer/FormationController（批A 触达）；ScriptStageMachine 阶段门槛（战士≥4+人口≥12+占区≥2）；GameEvents A6 事件；TrainingSystem.CanTrainGeneral L665-696（镜像玩家链在库）。
- 待侦察（施工前逐个直读）：TrainingSystem 训练队列系统级入口现状/RaceDef 结构与四资产/UtilityActionConfig.asset 行动数组/BuildingDef 军事建筑 id 清单（练兵场/战营/射箭场/战争学院/SiegeWorkshop）/ExecuteBuildFocus 现状/SiegeProductionSystem.ProduceMachine+L176/RecruitStandard 成军链。

## 五、教训引用兑现

| 教训 | 本批兑现 |
|------|---------|
| L-01（消费端断链） | B2/B3/B6 行动条目=AI 真消费（探针断言焦点选中+执行产出），非结构在场 |
| L-02（写后必验） | 每笔施工后磁盘直验+git diff 构成清点 |
| L-05（白名单盲区） | B5 归因日志/新 tag 列报（若产品面新增日志） |
| L-15（承接实体化） | 锚点施工前逐个直读，漂移/缺口如实列报 |
| L-17（容器入口） | 批B 探针容器一律 EnterTestRun 正门+ExitTestRun 收尾+15x |
| sim-sync（预登记） | B8 机器 AI 造机器 sim 侧无对应语义→15_账本登记（E2 同串，差距非阻断） |

执行端开工。下一步=锚点侦察→B1 起。
