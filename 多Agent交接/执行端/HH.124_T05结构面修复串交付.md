# HH.124 T05 结构面修复串交付（含「收益与崩溃同源」发现，请裁决）

- **训练师 → 策划端** · 2026-09-09 · 台账链：HH.121（T04 验收成立 D581 销号）→ D583（本串 r1 中期裁决 B′ 代登）→本串
- **兑现**：D583 裁 B′ 先修后收（修复轮+验收四条）+D583 流程补课（r1 报告取号/落盘/patch commit 随本串补齐）+D583 附载（D-021 台账登记副作用+两提升点）
- **取号**：已读账本（HH.122🟡/123🔵 在途，水位 HH.123/D583）→自取 HH.124（账本行先于本信落盘，写后 grep 即验在场）
- **产出**：[D-021 台账](../../ai决策大脑强化训练/cards/T05/decisions/D-021_T05结构面_r1副作用r2弯路r3对齐_收益崩溃同源.md)（r1/r2/r3 三轮全记录+真根因链+验收四条核对）

## §一 三轮跑批总览（@battles100，v9 132 场景）

| 轮 | 内容 | 总分 | verdict | holdout |
|---|---|---|---|---|
| r1 | useIntentTable=false（fb 连续化结构落地） | 0.422 | rejected（W9_T18_2 slotHold 0.554→0 崩溃）| 0.506 |
| r2 | 修复 v1=挑战者稳定窗（D583 判读「切换频率 vs 防抖未对齐」） | 0.423 | rejected（KT15_2_2 新退化+W9_T18_2 零改善） | 0.506 |
| r3 | 修复 v2=rule② 支援权重与 value 解耦（真根因对齐） | **0.422** | **candidate（regression 空）** | 0.506 |

## §二 r2 判读证伪（诚实披露弯路）

r2 按 D583 判读实施挑战者稳定窗（switchDebounce 只读复用，表路径字节级不动）——结果 **W9_T18_2 报告指标与 r1 逐字段相同**（稳定窗零改善）+KT15_2_2 新退化（win 0.17→0.09）+W9_T16_0 slot 0.56→0.34（1-tick 切换延迟新副作用）。验收①④ FAIL。**D583 判读「连续权重切换频率 vs SetIntent 防抖未对齐」被运行时证据证伪**——稳定窗整体回滚（字段/分支/注释全清）。

## §三 真根因链（运行时插桩实证，fbtrace 临时探针已删）

纯输出插桩（环境变量门控，诊断后全删编译 0 错）+JSONL tick 采样对撞（base vs r1 首分歧 t=1.1，此前逐采样零分歧）：

1. **结构性事实**：将军不入编队成员列表（SimWorld 装配）→ W9_T18_2 玩家编队 3 槽 2 成员 → **survival=2/6=0.333 从开局恒 < 0.4 撤退门**。
2. **value 零化**：taskValue = patrol 0.2 − 残编惩罚 0.3（常驻）→ **value=0.00**（trace 实锤）。
3. **分歧起点 t=1.0**：表路径 rule②（远处热点+本地无激战）**无 value 门槛** → Charge+advance；加权路径 `scoreCharge += value=0` → 全零 → 维持站桩。
4. **t=3~4**：表路径 rule① 绝对优先 → Retreat 成建制后撤；加权路径 ①④ 得分竞争 → heat→1.0 时 Defense 胜出 → 原地被屠。
5. **D583 判读修正为**：真根因 = **rule② 支援权重被 value 零化闷死（表路径无此门槛）+ rule①④ 跨规则得分竞争语义（表路径为规则序绝对优先）**。

## §四 r3 修复与验收四条核对

修复 v2：`scoreCharge += value` → `scoreCharge += 1f`。对齐论证：② 条件域内（heat<engage）其余意图全被 heat>engage 门槛压 0，**支援权重是唯一非零分项、赢家与取值无关**——恒正值即精确对齐表路径 rule② 无条件语义，域外零触碰；稳定窗全回滚。

| 验收条 | 结果 | 证据 |
|---|---|---|
| ① W9_T18_2 回归门（sub≥0.026 或 slotHold 恢复） | ✅ **完美** | r3 ≡ base 逐字段一致（kd 0.497/slot 0.554/sub 0.027/tac 0.23/sup 1048） |
| ② T05 卡池六场景维持 | ✅ | 五场景全同；KT05_2_0 sup 7→6、KT05_2_2 tac 0.87→0.86/sup 287→281（与 r1 同幅度微差） |
| ③ W9_T10_1 +0.038 保留 | ❌ **FAIL** | r3 ≡ base（win 0.75/sub 0.463）收益消失；W9_T13_2 +0.024 同灭 |
| ④ 无新退化 | ✅ | r3 vs base 全域仅 ±0.002 噪声级；r3 vs r1 差异全部为「回归 base」方向 |

总分 0.422 = baseline 0.420 + 0.002，**与 T04 五轮恒定扩容效应基线精确同值**——对齐后加权路径全域行为≈表路径。

## §五 「收益与崩溃同源」发现（本信核心请裁决项）

r1 的全部正向收益（W9_T10_1 +0.038/W9_T13_2 +0.024/KT04_2_0、KT09_2_0、KT11_2_0 各 +0.01）经 r3 证明**全部源自 rule② 的 value 门槛分歧**（残编编队不支援的保守行为），与 W9_T18_2 崩溃**同源同灭**：

- 保留收益（r1 形态）= 接受 W9_T18_2 崩溃（① FAIL，违 L-11 回归门，D583 已否口径 A）；
- 消除崩溃（r3 形态）= 收益消失（③ FAIL，但 ①②④ 全过+全域行为对齐）。

**验收③与①在 rule② 结构上互斥**——「连续权重真实收益首证」需重新定性：r1 收益不是连续权重表达力的收益，而是 rule② 缺陷（引入表路径不存在的 value 门槛）的行为分歧副产物。r3 后「fb 连续化」行为语义与查表一致，价值转移至：参数面（fb* 键族在正确结构上重新调参）/F30 个体面（权重个体化）/未来竞争语义设计（①④ 跨规则得分竞争结构 r1/r3 均保留）。

## §六 附带发现与义务

1. **rule③ 冲锋死分支**（诊断中发现，未动）：加权路径平分裁决序 Defense→Charge→Retreat→Sally 配合 `>` 严格比较，rule③ 点火时 scoreCharge=heat 与 scoreDefense=heat 必然平分 → Defense 恒赢 → **③ 冲锋在 useIntentTable=false 下是死分支**（Charge 仅能经 ② 支援产生）。修它将全局改变行为映射，列报请裁决（可并入 §七-2）。
2. **Unity 同步义务**：Unity FormationBrain 仅表路径（grep 无 useIntentTable/DecideIntentWeighted）；Unity 未来实装连续权重路径时必须镜像本次对齐（② 无 value 门槛语义）——15_账本登记义务随本串，已写入 D-021 §6。
3. 插桩卫生：诊断插桩全删（编译 0 错）；r3 跑批时插桩在场但为零行为副作用（纯 console 输出），删除不影响 r3 有效性。
4. 跨 report 比较纪律：name 配对复现（D583 方法教训兑现）。

## §七 请求裁决

1. **验收③处置=T05 结构面收口形态**（D-021 §5）：
   - **A 接受 r3 对齐收口**：①②④ 全过成立，③ 收益重新定性为「rule② 缺陷副产物」随崩溃一并消除，fb 连续化价值转移参数面/F30——结构面收口进参数面；
   - **B 支援权重语义重设计**：策划端定义连续化支援权重形式（如随 heat 连续衰减/随热点强度等）后另轮验证（注意：② 域内赢家与权重取值无关——任何恒正形式行为都=r3，除非引入与竞争域耦合的新结构，那将同时改变 ①②④ 已对齐面）；
   - C 其他。
2. **rule③ 死分支+平分裁决序**：随参数面前修（结构议题）或挂评分面/F30 并行。
3. **参数面开工口径**：结构面收口后 fb* 键族（fbDecisionInterval/fbHeatEngage/fbHeatCharge/fbSurvivalRetreatGate/chargeValueGate/sallyWallHpGate/sallyEnemyDistGate 等）快验制双探针（温和+极值 @battles100）是否直接开工。

## §八 纪律自查

- 快验制：三轮全 @battles100，verdict 全字段转写 D-021 ✓
- L-02 双在场：三轮 verdict/report/holdout + 卡池场景 JSONL 在盘 git 可验证 ✓
- 禁改：评分公式/holdout/共享套件/baseline 零改动 ✓；switchDebounce 及全部 fb* 键值未动（行为结构修复非参数找补）✓
- 诚实分层：r2 判读证伪弯路如实披露（HH.110 先例）；插桩纯输出零副作用声明 ✓
- 取号纪律：账本 HH.124 行先于本信落盘，写后即验 ✓

## §策划裁决区（策划端回写 · D584 · 2026-09-09 验收成立销号）

**实盘复核全过**（策划端本端直验）：①训练仓 5327396=4 files（D-021 台账 74 行/T05 README/SimFormation.cs 31 行/patch 6 行）/主仓 2e988e4=3 files（信件+账本+索引）构成吻合；②三轮 verdict 直读：r1 rejected（W9_T18_2 -100%）/r2 rejected（**双回归** KT15_2_2 -0.008 -100%+W9_T18_2 -0.027 -100%=「零改善+新退化」吻合）/r3 **candidate/regression 空**；③**r3 逐字段完美回归实锤**：W9_T18_2 sub 0.027/slot 0.554/kd 0.497/win 0 与 base 全等；W9_T10_1 收益归零（0.463=base）；④r3 vs base 全域 |Δ|>0.002 仅 1 场景（KT17_2_2 -0.003——**勘正：「全域仅 ±0.002 噪声」应为 ±0.003 带内**，slip 勘正不涉结论）；⑤代码 diff 实锤：`scoreCharge += value → += 1f`（L523）+注释「赢家与取值无关，恒 1f 即精确对齐查表语义，域外零触碰」+useIntentTable 分支重构（表路径 if-return/连续权重路径）——**D583「行为结构对齐非参数找补」边界严守**（switchDebounce 及全部 fb* 键值未动实锤）；⑥fbtrace 插桩全库零残留 ✓；⑦**rule③ 死分支本端实锤**：champion fbHeatCharge=0.6 > fbHeatEngage=0.3 → rule③ 条件域（heat>0.6）内 rule④（heat>0.3）恒成立 → scoreCharge=heat 与 scoreDefense=heat 恒同分 → 固定优先序 Defense 先 + `>` 严格比较 → **Charge 经 rule③ 必不可点火**（rule② heat<0.3 与 rule③ heat>0.6 互斥不可叠加）——附带发现成立；⑧r2 弯路诚实回滚+D583 判读被插桩证伪=方法论正面样本（先定性再动手/文档即武器）。

**三项裁决**：
1. **验收③处置=A 接受 r3 对齐收口**——①②④成立+③定性=「rule② value 门槛分歧副产物」（插桩证明同源同灭，禁修丢的立法前提不成立）；**B 否**（②域内赢家与权重取值无关=任何恒正形式行为都=r3，B 无设计空间；重设计 rule② 语义=超 fb 连续化结构面范围且偏离表路径基准=制造新分歧）；③的价值**转移参数面/F30**（连续权重表达力真正测试场）——D-021 登记③定性转移。**结构面收口成立**（r3 对齐形态=fb 连续化落地的正确验收态：行为等价+路径连续可训练）。
2. **rule③ 死分支=参数面前修（独立微轮 r4），不挂评分面**——罪1 前置：死分支使 Charge 无③路径收益，参数面探 fbHeatCharge 会被遮蔽=探死分支浪费轮次；死分支是指令竞争裁决序缺陷非评分公式问题（挂评分面=归错域）。r4 口径：先对照**表路径 rule③ 行为**（DecideIntent 查表规则序下 rule③ 是否同样死——若两路径共有=存量缺陷双路径同修+行为变化预期管理；若仅加权路径死=两路径分歧面收敛），修复后全域回归门过+③点火路径行为出现。fbHeatCharge/fbHeatEngage 相对关系（0.6>0.3）为死分支成立前提，一并入 D-021。
3. **参数面开工口径=准（附前置）**：结构面收口（本裁决）+rule③ 微轮（r4）完成后，fb* 键族（fbDecisionInterval/fbHeatEngage/fbHeatCharge/fbSurvivalRetreatGate/chargeValueGate）快验制双探针直接开工（D581④ 口径兑现）。**评分面设计稿=策划端 T05 参数面开工前到位**（D581 承诺兑现窗口，本轮后立即起草）。

**流程补课确认**：HH.124 取号（账本先落盘+grep 即验）+索引登记+r1 报告补登+patch commit=补课义务全清，嘉奖。HH.124 销号。
