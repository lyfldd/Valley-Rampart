# 1.3 EventBus（泛型事件系统）

### 优先级：🔴 最高（所有系统都依赖它）

### 脚本

| 文件 | 路径 | 说明 |
|------|------|------|
| `EventBus.cs` | `Assets/_Game/Core/` | 泛型事件总线，静态类 |
| `GameEvents.cs` | `Assets/_Game/Core/` | 所有事件 struct 定义 |

### EventBus 核心设计

```
EventBus 是静态泛型类：
  - 内部维护 Dictionary<Type, Delegate> 订阅表
  - Subscribe<T>(Action<T> handler)   → 注册监听
  - Unsubscribe<T>(Action<T> handler) → 移除监听
  - Publish<T>(T eventData)           → 广播事件（struct 传值，无 GC）
```

### 关键实现要点

1. **使用 `struct` 定义事件，不用 `class`**——避免 GC 分配
2. **所有事件 struct 标记 `readonly`**——防止误改
3. **Publish 时遍历 Delegate.GetInvocationList()**——逐个调用，某个 handler 抛异常不影响其他
4. **不需要** `MonoBehaviour` 依赖——纯 C# 静态类

### 事件定义清单

> **已实现事件**（17 个有效 struct + 5 个枚举，见 `GameEvents.cs`）—— 对应已落地系统。
> **规划中事件**（⬜ 待实现系统落地时再定义）—— 保留作为后续设计参考。

#### ✅ 已实现事件（GameEvents.cs 中定义）

| 事件 struct | 字段 | 发布方 | 订阅方 |
|-------------|------|--------|--------|
| `PlayerMoveEvent` | `Vector3 position, moveDir` | InputManager | PlayerInputHandler |
| `GameStateChangedEvent` | `GameState OldState, NewState` | GameStateManager | 所有 UI 面板 |
| `UnitDiedEvent` | `UnitController Unit` | UnitController | RulerController（君主阵亡检测） |
| `UnitSpawnedEvent` | `UnitController Unit` | UnitController | UI / 仇恨系统 |
| `UnitHpChangedEvent` | `UnitController Unit, int OldHp/NewHp/MaxHp` | UnitController | TopLeftHUD（血条） |
| `RulerResourceChangedEvent` | `ResourceType Type, int OldValue/NewValue` | RulerController | ResourceHUD |
| `UnitAttackEvent` | `UnitController Attacker/Target, int RawDamage` | UnitController | 战斗系统扩展 |
| `UnitDamagedEvent` | `UnitController Unit/Source, int ActualDamage` | 战斗结算 | UI / 特效 |
| `UnitAttributeChangedEvent` | `UnitController Unit, UnitAttributeType AttributeType` | Buff/装备 | UI |
| `TimeDayChangedEvent` | `int OldDay/NewDay, Season Season` | TimeManager | SaveManager（自动存档） |
| `TimePhaseChangedEvent` | `TimePhase OldPhase/NewPhase` | TimeManager | 敌人刷新 / 光照 |
| `SeasonChangedEvent` | `Season OldSeason/NewSeason` | TimeManager | DifficultyManager（每季 +难度） |
| `DifficultyChangedEvent` | `float OldFactor/NewFactor` | DifficultyManager | WaveManager / 战斗消费 |
| `GameSavedEvent` | `string SlotId, bool IsSuccess` | SaveManager | UI |
| `GameLoadedEvent` | `string SlotId, bool IsSuccess` | SaveManager | UI |
| `ConfigsLoadedEvent` | `bool IsSuccess` | LoadManager | GameBootstrap |
| `EscapePressedEvent` | `GameState CurrentState` | InputManager | PausePanel |

**配套枚举**（同文件）：
- `ResourceType`: Gold / Stone / Wood / Food / Ore / Crystal / Oil / Bread / Meat / Metal
  - **10 种全清单（2026-08-14 定稿，D199）**：基础 金/石/木/粮/矿 + 副产 水晶/火油 + 加工 面包/肉 + 新增 **Metal（铁匠铺 D127，兵种强化+工事升级 D131）**。加工品（面包/肉/金属）为**独立资源类型**，可搬运/装箱（D142）/存储/交易。旧 1D 仅 金/石/木/粮 已扩展。
- `UnitAttributeType`: MaxHp / Attack / Defense / WalkSpeed / RunSpeed
- `TimePhase`: Night / Dawn / Day / Dusk
- `Season`: Spring / Summer / Autumn / Winter
- `GameState`: Booting / Splash / MainMenu / CharacterCreation / SaveSlotSelect / Loading / Ready / Playing / Paused / GameOver

> ⚠️ `UnitDataLoadedEvent` 已废弃（保留仅为兼容），新代码统一用 `ConfigsLoadedEvent`。

#### ⬜ 规划中事件（待实现系统落地时定义）

| 事件 struct | 预留用途 | 依赖系统 |
|-------------|----------|----------|
| `BuildingPlacedEvent` | 建造放置完成 | 建造系统 ⬜ |
| `BuildingDestroyedEvent` | 建筑被摧毁 | 战斗系统扩展 ⬜ |
| `WaveCompletedEvent` | 一波敌人清完 | 敌人波次 ⬜ |
| `FactionRelationChangedEvent` | 阵营好感变化 | 动态阵营 ⬜ |
| `DisasterTriggeredEvent` | 天灾触发 | 天灾系统 ⬜ |
| `ExpeditionStartedEvent` | 跨岛远征开始 | 跨岛远征 ⬜ |

> ❌ **已废弃事件**（不再实现）：
> - `DefenseLineBreachedEvent`（壕沟防线突破）—— 2.0_v2 删除地形改造系统后废弃
> - `TerrainModifiedEvent`（地形改造）—— 同上
> - `DayNightChangedEvent` —— 被 `TimePhaseChangedEvent` 替代
> - `ResourceChangedEvent` —— 被 `RulerResourceChangedEvent` 替代
> - `GameOverEvent` —— 改用 `GameStateChangedEvent(GameOver)` 替代

### 验证标准
- [ ] 新建空场景，挂一个测试脚本：Subscribe → Publish → 回调被调用
- [ ] 同一个事件多个订阅者，全部收到
- [ ] Unsubscribe 后不再收到
- [ ] Publish 时其中一个 handler 抛异常，其他 handler 正常执行

---

>> 📌 **2026-08-30 漂移追记**（转录自 [文档对账报告_2026-08-23](./报告/落实对账/文档对账报告_2026-08-23.md) §三 及 _目录 状态列后续裁决；仅转录未重审代码，最后核对日期以 _目录 为准）：
> - 事件数 17（文内）→ 36（2026-08-23）→ 43（2026-08-29 Q6 复测）；GuardRegionLostEvent 定义双源在 GuardDeploymentSystem.cs:37（迁移涉及代码，随对应批）。

> _返回 [主目录](./_目录.md)（阶段1目录已归档 2026-08-30）_
