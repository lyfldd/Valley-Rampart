> ⚰️ **墓碑（2026-08-23 立）**
> 本文档已被 [改造计划/2_10_渲染与摄像机.md](../改造计划/2_10_渲染与摄像机.md) 与 [改造计划/美术资源规范_等轴立方体瓦片.md](../改造计划/美术资源规范_等轴立方体瓦片.md) 取代（占位已 2D 化：等轴 128×64 / PPU100 / pivot 底面中心，Systems/Rendering/PlaceholderSprites）。
> 内容仅作历史快照，**禁止作为实施依据**，引用请指向继任文档。

# 🎨 美术占位方案（Unity 2D 原始组件占位）

> 2026-07-27 阿铁拍板，主理人游承峰汇编
> 美术资源没到位时，用代码生成的纯色 sprite 占位，先做出来再替换

## 📝 占位原则

1. **零美术依赖**——不依赖任何 PNG/PSD，Unity 工程打开就能跑
2. **颜色+形状区分**——不同单位/建筑用不同颜色 + 形状（方块/圆形/三角）
3. **文字标签辅助**——关键单位加 TextMeshPro 文字标签（"君"/"兵"/"塔"）
4. **代码生成 sprite**——用 `Sprite.Create + Texture2D` 程序生成，缓存复用
5. **替换路径预留**——正式美术到位后，只改 `PlaceholderSprites` 一个类，全游戏自动替换

---

## 一、占位手段对比

| 手段 | 优点 | 缺点 | 是否采用 |
|------|------|------|:--:|
| **代码生成纯色 sprite** | 零依赖，打开即用，替换方便 | 不好看 | ✅ 主力 |
| Unity 自带 UI sprite | 免费 | 太少，不够区分 | ❌ |
| 手画简单 PNG | 直观 | 要画图软件，费时 | ❌（备选） |
| Asset Store 免费 sprite 包 | 好看 | 要找+导入 | ❌（未来可选） |

**结论**：用代码生成纯色 sprite，完全不依赖外部资源。等美术到位再替换。

---

## 二、占位资源清单（颜色 + 形状 + 标签）

### 2.1 单位类

| key | 名称 | 形状 | 大小 | 颜色 | 标签 |
|-----|------|------|:--:|------|:--:|
| `monarch` | 君主 | 方块 | 64 | 蓝 (0.2,0.4,0.8) | 君 |
| `villager` | 村民 | 方块 | 32 | 黄 (0.9,0.8,0.2) | 民 |
| `monster_small` | 小怪 | 三角▼ | 32 | 红 (1,0,0) | — |
| `monster_boss` | BOSS | 方块 | 64 | 深红 (0.5,0,0) | 王 |
| `soldier_infantry` | 王国步兵 | 方块 | 32 | 深红 (0.6,0.1,0.1) | 兵 |
| `soldier_archer` | 王国弓手 | 方块 | 32 | 橙 (0.9,0.5,0) | 弓 |
| `soldier_cavalry` | 王国骑兵 | 方块 | 32 | 紫 (0.5,0.2,0.6) | 骑 |

### 2.2 建筑类

| key | 名称 | 形状 | 大小 | 颜色 | 标签 |
|-----|------|------|:--:|------|:--:|
| `tower_arrow` | 箭塔 | 细长方块 | 32×64 | 绿 (0.2,0.6,0.2) | 塔 |
| `tower_catapult` | 投石机 | 方块 | 48 | 深绿 (0.1,0.4,0.1) | 投 |
| `trap` | 陷阱 | 三角▼ | 32 | 暗黄 (0.6,0.5,0.1) | — |
| `wall` | 城墙 | 粗方块 | 32 | 灰 (0.5,0.5,0.5) | — |
| `farm` | 农场 | 方块 | 48 | 黄绿 (0.7,0.8,0.2) | 田 |
| `mine` | 矿洞 | 圆形 | 48 | 深灰 (0.3,0.3,0.3) | 矿 |
| `lumbermill` | 伐木场 | 方块 | 48 | 深绿 (0.1,0.4,0.1) | 木 |
| `shipyard` | 造船厂 | 方块 | 64 | 蓝绿 (0.2,0.6,0.6) | 船 |
| `castle` | 主城 | 大方块 | 128 | 金 (0.9,0.8,0.2) | 城 |

### 2.3 投射物 + 特殊

| key | 名称 | 形状 | 大小 | 颜色 |
|-----|------|------|:--:|------|
| `projectile_arrow` | 箭矢 | 小方块 | 4 | 白 |
| `projectile_stone` | 石头 | 小圆 | 8 | 灰 |
| `rift` | 裂隙（刷怪点） | 圆形 | 64 | 紫黑 (0.3,0,0.3) |
| `unknown` | 未配置警告 | 方块 | 32 | 品红（magenta） |

### 2.4 地形 Tilemap（单色）

| 地形 | 颜色 |
|------|------|
| 平原 | 浅绿 (0.7,0.9,0.5) |
| 肥沃 | 黄绿 (0.8,0.9,0.3) |
| 荒地 | 黄 (0.9,0.8,0.4) |
| 丘陵 | 棕 (0.6,0.4,0.2) |
| 林地 | 深绿 (0.2,0.5,0.2) |
| 矿山 | 灰 (0.4,0.4,0.4) |
| 雪山 | 白 (0.9,0.9,0.95) |

---

## 三、完整代码

### 3.1 SpriteFactory.cs（sprite 生成工具）

**文件**：`Assets/_Game/Core/SpriteFactory.cs`

```csharp
using UnityEngine;

/// <summary>
/// Sprite 生成工具。用代码生成纯色 sprite，不依赖美术资源。
/// 美术到位后，改 PlaceholderSprites.Get 改为 Resources.Load 即可全替换。
/// </summary>
public static class SpriteFactory
{
    /// <summary>生成纯色方块 sprite。</summary>
    public static Sprite CreateSquare(int size, Color color)
    {
        Texture2D tex = new Texture2D(size, size);
        Color[] pixels = new Color[size * size];
        for (int i = 0; i < pixels.Length; i++) pixels[i] = color;
        tex.SetPixels(pixels);
        tex.Apply();
        return Sprite.Create(tex, new Rect(0, 0, size, size),
            new Vector2(0.5f, 0.5f), pixelsPerUnit: size);
    }

    /// <summary>生成纯色圆形 sprite。</summary>
    public static Sprite CreateCircle(int size, Color color)
    {
        Texture2D tex = new Texture2D(size, size);
        Color[] pixels = new Color[size * size];
        float center = size / 2f;
        float radius = size / 2f;
        for (int y = 0; y < size; y++)
        {
            for (int x = 0; x < size; x++)
            {
                float dist = Vector2.Distance(new Vector2(x, y), new Vector2(center, center));
                pixels[y * size + x] = dist <= radius ? color : Color.clear;
            }
        }
        tex.SetPixels(pixels);
        tex.Apply();
        return Sprite.Create(tex, new Rect(0, 0, size, size),
            new Vector2(0.5f, 0.5f), pixelsPerUnit: size);
    }

    /// <summary>生成向下尖的三角 sprite（用于怪物/陷阱）。</summary>
    public static Sprite CreateTriangle(int size, Color color)
    {
        Texture2D tex = new Texture2D(size, size);
        Color[] pixels = new Color[size * size];
        for (int y = 0; y < size; y++)
        {
            // 从底部宽到顶部窄
            int halfWidth = (int)((size - y) / 2f);
            for (int x = 0; x < size; x++)
            {
                bool inTriangle = x >= halfWidth && x < size - halfWidth;
                pixels[y * size + x] = inTriangle ? color : Color.clear;
            }
        }
        tex.SetPixels(pixels);
        tex.Apply();
        return Sprite.Create(tex, new Rect(0, 0, size, size),
            new Vector2(0.5f, 0.5f), pixelsPerUnit: size);
    }

    /// <summary>生成细长方块（用于箭塔）。</summary>
    public static Sprite CreateRect(int width, int height, Color color)
    {
        Texture2D tex = new Texture2D(width, height);
        Color[] pixels = new Color[width * height];
        for (int i = 0; i < pixels.Length; i++) pixels[i] = color;
        tex.SetPixels(pixels);
        tex.Apply();
        return Sprite.Create(tex, new Rect(0, 0, width, height),
            new Vector2(0.5f, 0.5f), pixelsPerUnit: Mathf.Max(width, height));
    }
}
```

### 3.2 PlaceholderSprites.cs（占位资源注册中心）

**文件**：`Assets/_Game/Core/PlaceholderSprites.cs`

```csharp
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 占位 sprite 注册中心。按 key 获取，自动缓存。
/// 美术到位后：把 Get 改为先 Resources.Load("Art/" + key)，找不到再生成占位。
/// </summary>
public static class PlaceholderSprites
{
    private static readonly Dictionary<string, Sprite> _cache = new Dictionary<string, Sprite>();

    /// <summary>按 key 获取 sprite。优先从缓存取，没有则生成。</summary>
    public static Sprite Get(string key)
    {
        if (_cache.TryGetValue(key, out Sprite s)) return s;
        s = CreatePlaceholder(key);
        _cache[key] = s;
        return s;
    }

    /// <summary>预生成所有占位 sprite（启动时调一次，避免运行时卡顿）。</summary>
    public static void PreloadAll()
    {
        string[] keys = {
            "monarch", "villager", "monster_small", "monster_boss",
            "soldier_infantry", "soldier_archer", "soldier_cavalry",
            "tower_arrow", "tower_catapult", "trap", "wall",
            "farm", "mine", "lumbermill", "shipyard", "castle",
            "projectile_arrow", "projectile_stone", "rift", "unknown"
        };
        foreach (var key in keys) Get(key);
        Debug.Log($"[PlaceholderSprites] 预生成 {keys.Length} 个占位 sprite");
    }

    private static Sprite CreatePlaceholder(string key)
    {
        // 蓝色系：君主
        // 黄色系：村民/农场
        // 红色系：怪物/王国兵
        // 绿色系：防御建筑/伐木场
        // 灰色系：城墙/矿洞
        // 紫黑：裂隙
        // 金色：主城
        switch (key)
        {
            // ===== 单位 =====
            case "monarch":           return SpriteFactory.CreateSquare(64, new Color(0.2f, 0.4f, 0.8f));
            case "villager":          return SpriteFactory.CreateSquare(32, new Color(0.9f, 0.8f, 0.2f));
            case "monster_small":     return SpriteFactory.CreateTriangle(32, Color.red);
            case "monster_boss":      return SpriteFactory.CreateSquare(64, new Color(0.5f, 0f, 0f));

            // ===== 王国兵（多兵种）=====
            case "soldier_infantry":  return SpriteFactory.CreateSquare(32, new Color(0.6f, 0.1f, 0.1f));
            case "soldier_archer":     return SpriteFactory.CreateSquare(32, new Color(0.9f, 0.5f, 0f));
            case "soldier_cavalry":   return SpriteFactory.CreateSquare(32, new Color(0.5f, 0.2f, 0.6f));

            // ===== 防御建筑 =====
            case "tower_arrow":       return SpriteFactory.CreateRect(32, 64, new Color(0.2f, 0.6f, 0.2f));
            case "tower_catapult":    return SpriteFactory.CreateSquare(48, new Color(0.1f, 0.4f, 0.1f));
            case "trap":              return SpriteFactory.CreateTriangle(32, new Color(0.6f, 0.5f, 0.1f));
            case "wall":              return SpriteFactory.CreateSquare(32, new Color(0.5f, 0.5f, 0.5f));

            // ===== 资源建筑 =====
            case "farm":              return SpriteFactory.CreateSquare(48, new Color(0.7f, 0.8f, 0.2f));
            case "mine":              return SpriteFactory.CreateCircle(48, new Color(0.3f, 0.3f, 0.3f));
            case "lumbermill":        return SpriteFactory.CreateSquare(48, new Color(0.1f, 0.4f, 0.1f));
            case "shipyard":          return SpriteFactory.CreateSquare(64, new Color(0.2f, 0.6f, 0.6f));
            case "castle":            return SpriteFactory.CreateSquare(128, new Color(0.9f, 0.8f, 0.2f));

            // ===== 投射物 + 特殊 =====
            case "projectile_arrow":  return SpriteFactory.CreateSquare(4, Color.white);
            case "projectile_stone":  return SpriteFactory.CreateCircle(8, Color.gray);
            case "rift":              return SpriteFactory.CreateCircle(64, new Color(0.3f, 0f, 0.3f));

            // ===== 未配置警告 =====
            default:
                Debug.LogWarning($"[PlaceholderSprites] 未配置 key: {key}，用品红方块占位");
                return SpriteFactory.CreateSquare(32, Color.magenta);
        }
    }
}
```

### 3.3 用法示例

```csharp
// ===== 单位创建（UnitFactory 或代码兜底）=====
GameObject go = new GameObject("monster_small");
SpriteRenderer sr = go.AddComponent<SpriteRenderer>();
sr.sprite = PlaceholderSprites.Get("monster_small");
// 可选：加文字标签
// var label = go.AddComponent<TextMeshPro>(); label.text = "怪";

// ===== 投射物创建（Projectile 系统）=====
GameObject proj = new GameObject("arrow");
SpriteRenderer sr = proj.AddComponent<SpriteRenderer>();
sr.sprite = PlaceholderSprites.Get("projectile_arrow");

// ===== 建筑创建（BuildController）=====
GameObject building = new GameObject("tower_arrow");
SpriteRenderer sr = building.AddComponent<SpriteRenderer>();
sr.sprite = PlaceholderSprites.Get("tower_arrow");

// ===== 启动时预生成（LoadManager 阶段1）=====
PlaceholderSprites.PreloadAll();  // 加到 LoadManager.LoadStaticConfigs 里
```

---

## 四、文字标签（可选，关键单位加）

对于需要快速识别的单位（君主/王国兵/主城），在 sprite 上叠加 TextMeshPro 文字标签：

```csharp
// 给单位加文字标签的辅助方法
public static void AddLabel(GameObject parent, string text, float yOffset = 0.5f)
{
    GameObject labelObj = new GameObject("Label");
    labelObj.transform.SetParent(parent.transform, false);
    labelObj.transform.localPosition = new Vector3(0, yOffset, 0);
    var tmp = labelObj.AddComponent<TextMeshPro>();
    tmp.text = text;
    tmp.fontSize = 4;
    tmp.alignment = TextAlignmentOptions.Center;
    tmp.color = Color.white;
}
```

建议加标签的：
- 君主："君"
- 王国兵：兵/弓/骑（区分兵种）
- 主城："城"
- 裂隙："裂"

怪物/陷阱/投射物不加标签（数量多，标签会乱）。

---

## 五、占位 → 正式资源替换路径

### 5.1 替换流程

```
现在（美术未到位）
  │  PlaceholderSprites.Get(key) → 代码生成纯色 sprite
  ▼
美术资源到位
  │  把正式 sprite 放到 Resources/Art/ 下（如 Resources/Art/monarch.png）
  ▼
改 PlaceholderSprites.Get 的实现
  │  先 Resources.Load<Sprite>("Art/" + key)，找不到再生成占位
  ▼
最终全游戏自动替换为正式美术
```

### 5.2 替换时只改一个方法

```csharp
// 改后的 Get 方法（美术到位后）
public static Sprite Get(string key)
{
    if (_cache.TryGetValue(key, out Sprite s)) return s;

    // 优先尝试加载正式美术资源
    Sprite official = Resources.Load<Sprite>($"Art/{key}");
    if (official != null)
    {
        _cache[key] = official;
        return official;
    }

    // 找不到正式资源，回退到占位
    s = CreatePlaceholder(key);
    _cache[key] = s;
    return s;
}
```

**关键**：消费方代码（UnitFactory/BuildController/Projectile）**零改动**——它们只调 `PlaceholderSprites.Get(key)`，不关心是占位还是正式。

### 5.3 替换检查表

美术到位后，逐个替换并检查：

| key | 正式资源路径 | 替换状态 |
|-----|-------------|:--:|
| monarch | Resources/Art/monarch.png | ⬜ |
| villager | Resources/Art/villager.png | ⬜ |
| monster_small | Resources/Art/monster_small.png | ⬜ |
| ... | ... | ⬜ |

---

## 六、和现有系统的衔接

| 现有系统 | 占位方案怎么接 |
|---------|--------------|
| **UnitFactory** | SpawnUnit 时，给 GameObject 加 SpriteRenderer，sprite 从 PlaceholderSprites.Get 取 |
| **LoadManager** | 阶段1 LoadStaticConfigs 调 PlaceholderSprites.PreloadAll() 预生成 |
| **BuildingData SO**（未实现） | 加 spriteKey 字段，建造时用 PlaceholderSprites.Get(spriteKey) |
| **Projectile**（未实现） | 创建时用 PlaceholderSprites.Get("projectile_arrow"/"projectile_stone") |

---

## 七、实施批次

| 批次 | 内容 |
|------|------|
| 第1批 | 创建 `SpriteFactory.cs` + `PlaceholderSprites.cs` |
| 第2批 | LoadManager.LoadStaticConfigs 加 `PlaceholderSprites.PreloadAll()` |
| 第3批 | UnitFactory.SpawnUnit 用 PlaceholderSprites 给单位加 sprite |
| 第4批 | 未来建造系统/投射物系统创建时用 PlaceholderSprites |

> _2026-07-27 阿铁拍板，主理人游承峰汇编_
