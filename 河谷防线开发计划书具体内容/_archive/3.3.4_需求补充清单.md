> ⚰️ **墓碑（2026-08-30 立）**
> 14 项决策已并入 3.3.4 审查文档（同批归档）及后续 2_2/2_12 实施。
> 内容仅作历史快照，**禁止作为实施依据**。

# 📋 3.3.4 建造系统需求补充清单

> 2026-07-30 基于 3.3.4 原文档的需求讨论成果整理。
> 本文是对原文档 `3.3.4_建造系统审查问题与产能系统补全.md` 的**需求层补充**，
> 覆盖原 14 个问题的决策、讨论中新增的架构需求、待定后续需求。
> 用途：供审阅确认无遗漏后，再决定如何并入 3.3.4 文档。

---

## 一、原 14 个问题决策汇总

| # | 问题 | 决策结果 |
|---|------|---------|
| 1 | Building 未实现 ISaveable | 组件化后存档范围扩大，见【五、存档需求】 |
| 2 | 建筑产能逻辑缺失 | 集中调度器 + ProducerComponent，见【三、产能系统】 |
| 3 | 缺少 R 键旋转 | 实现，配合多格占地，见【四、建造系统】 |
| 4 | InteractionManager 缺悬停高亮 | SpriteRenderer.color 加亮（首版简单方案） |
| 5 | Die 缺 TeardownManager | 首版瞬间删除 + 按HP比例返还资源 |
| 6 | 敌方建筑交互未分化 | **玩家不可交互敌方建筑**，点击无响应（或浮动"敌方"提示） |
| 7 | OpenSubmenu 未实现 | 后期，依赖更多系统 |
| 8 | footprint 只用一维 | 改为 Vector2Int 二维，支持混合尺寸，见【四、建造系统】 |
| 9 | 建造菜单点击任意按钮都关闭 | 方案A：InteractionManager 检查 `UIManager.HasPanelOpen`，面板打开时跳过世界点击 |
| 10 | 四大资源产出端缺失 | 方案B存储 + 集中调度，见【三、产能系统】 |
| 11 | 建筑功能未组件化 | Component Pattern，本轮做 Producer+Storage，见【二、组件化架构】 |
| 12 | 建筑美术资源占位缺失 | 用 PlaceholderSprites 生成彩色方块占位 |
| 13 | BuildController 不在场景里 | SampleScene 显式放置 |
| 14 | 玩家可建造建筑只有 2 种 | 补充伐木场/采石场/农场/市场/投石机/魔法塔/城门 |

---

## 二、组件化架构（问题 11 展开）

### 2.1 Building 核心职责（瘦身）

Building 类只管：基础状态 + IInteractable 交互入口 + 生命周期。
具体行为拆成独立 Component 挂在同一 GameObject。

### 2.2 组件清单与边界

| 组件 | 本轮处理 | 触发条件 | 职责 |
|------|---------|---------|------|
| ProducerComponent | ✅ 实现 | `def.producer != null && kind == Resource` | 每秒产出资源写入本地存储 |
| StorageComponent | ✅ 实现 | 产出建筑必需 | storedAmount/capacity，满载停产出，预留搬运接口 |
| PickupComponent | ⚠️ 留接口 | `def.isConsumable == true` | 一次性采集（宝箱/木头堆），暂不实现 |
| SpawnerComponent | ⚠️ 留接口 | `def.producer.kind == Unit` | 产兵，依赖单位系统 |
| CombatComponent | ❌ 不碰 | `def.combat.attack > 0` | 依赖 3.4/3.5 |
| RiftComponent | ❌ 不碰 | `sourceType == Rift` | 依赖 3.7 波次 |
| CastleCoreComponent | ⚠️ 留接口 | `sourceType == CastleCore` | 主城解锁逻辑见【八、主城设计】 |

### 2.3 组件接口约定

- 定义 `IBuildingComponent` 接口框架，BuildingFactory 知道怎么挂组件
- 组件从 `BuildingDef` 读配置，不自己持有状态
- 留接口 = 定义接口和挂载逻辑，具体组件类先不实现

---

## 三、产能系统（问题 10 展开）

### 3.1 调度机制：集中调度器

```
ProductionSystem（新建，单例）
  ├─ 每秒统一遍历所有 ProducerComponent
  ├─ 支持暂停（游戏暂停时停 tick）
  ├─ 易存档（只存 StorageComponent.storedAmount）
  └─ 性能好（O(n) 遍历，无每帧 Update）
```

### 3.2 存储方案：方案B（本地存储 + 预留搬运接口）

```
ProducerComponent.Update（被 ProductionSystem 调用）
  → 检查 Building.state == Active
  → 检查 StorageComponent.storedAmount < capacity
  → 每秒 rate × gradeScale 产出
  → 写入 StorageComponent.storedAmount
  → 满载停产出

搬运接口（预留，不实现调用方）：
  IHarvestable 接口
    bool IsReadyToHarvest()  // storedAmount >= harvestThreshold
    int Harvest()            // 取走资源，返回取走量
  未来 NPC 工人轮询所有 IHarvestable，达阈值则搬运
```

### 3.3 资源点 + 工具模型（新增核心设计）

**设计理念**：资源点是前置条件，工具是生产设备，两者结合才能产出。

```
地图生成 → 预置持续性资源点（原始矿洞/原始树林/原始农田）
              ↓ 资源点自身不产出（仅前置）
         玩家在资源点上放置"生产工具"
              ↓ 走 Constructing 进度（脚手架）
         建造完成 → 资源点被改造为产出建筑
              ↓ ProducerComponent 启动产出
         StorageComponent 本地存储
              ↓ 等搬运（接口预留）
         （未来）NPC 工人搬运 → 国库
```

**关键规则**：
- 资源点 BuildingDef 标记 `isResourceNode=true`，自身不产出
- 工具→资源点映射表（ResourceNodeMapping）：采石场↔矿洞、伐木场↔树林、农场↔农田
- PlacementValidator 新增校验：产出建筑放置时检查目标格是否有对应资源点
- 放置即改造：建造完成后，原资源点 Building 被替换为产出建筑
- 资源点改造走 Constructing 进度，和建造统一

### 3.4 占位数值表

| 资源 | 初始量 | 产出 rate | capacity | 造价 |
|------|--------|----------|----------|------|
| 木 | 100 | 15/秒 | 100 | 50 |
| 粮 | 100 | 15/秒 | 100 | 50 |
| 石 | 100 | 5/秒 | 100 | 50 |
| 金 | 100 | 5/秒 | 100 | 50 |
| 升级倍率 | - | ×1.5/级 | ×1.2/级 | ×2/级 |
| 等级上限 | - | 3 级 | 3 级 | 3 级 |

### 3.5 资源平衡设计

**首版**：所有资源点永久产出，用速率差异做平衡（木粮快、石矿慢）。
**后期深化**：
- 种树：伐木场消耗粮+时间种植新树林资源点（木可再生）
- 炼金：科技解锁，把木/粮转化为石/矿（用可再生换稀缺）
- 地图扩张：随波次推进，地图边缘生成新资源点

---

## 四、建造系统（问题 3/8/12/13/14 展开）

### 4.1 建筑生命周期状态机（新增，组件化底层依赖）

```
Placing → Constructing → Active → Damaged → Dead
                ↑           |        |
                └───────────┘        |
                  升级               维修（未来）
                （走 Constructing）
```

**状态说明**：
| 状态 | 说明 | 组件行为 |
|------|------|---------|
| Placing | 放置中（ghost） | 无 |
| Constructing | 建造中（脚手架） | 不产出/不战斗 |
| Active | 活跃 | 产出/战斗/可交互 |
| Damaged | 受损（HP低） | 仍产出但效率降低？（首版不区分，等同 Active） |
| Dead | 死亡 | 触发 Die()，待销毁 |

**新增状态**：`Abandoned`（废弃），用于主城初始形态，见【八、主城设计】。

### 4.2 建造进度系统（统一架构）

**一处实现，三处复用**：建造 / 升级 / 维修 都走 `Constructing` + 进度条。

```
Constructing 状态的进度推进（双模式）：
  模式A（首版占位）：自动累计，每秒 +20%，5秒完成
  模式B（3.10后）：需要工人走到旁边才累计，无工人暂停

视觉：
  放置后立即显示半透明骨架/脚手架（占位 Sprite）
  完成时替换为正式建筑 Sprite + state=Active
  升级时贴图换脚手架，但 storedAmount/level 等数据保留不丢

打断：
  玩家可随时拆除在建建筑，按HP比例返还资源
```

### 4.3 多格占地 + R 键旋转

- `footprint: Vector2Int`（width × height），支持混合尺寸（1×1/2×2/2×3）
- R 键旋转 90°：w/h 互换
- PlacementValidator 遍历所有占用格校验（occupancy/terrain/boundaries）
- GridSystem.MarkOccupied 标记所有格
- ghost 预览显示多格半透明区域
- 吸附基准：左上角对齐小区块网格

### 4.4 建造手感优化

- **小区块网格吸附**：ghost 吸附到大区块内 16 个小区块的最近格
- **建造菜单自动隐藏**：进入建造模式后菜单隐藏，结束返回
- **建造后返回菜单**：单次放置返回菜单，Shift 连续放置保持模式
- **ghost 视觉反馈**：绿色可建/红色不可建

### 4.5 建造菜单 UI

- 布局：图标 + 名称 + 造价，资源不足灰显
- tab 分类：Production / Economy / Defense / Wall / Special
- tab 内布局：网格排列，超出滚动

### 4.6 建筑美术占位（问题 12）

用 PlaceholderSprites 生成彩色方块：
- 树→绿、矿洞→灰、农田→黄、箭塔→蓝、城墙→深灰
- 裂隙→紫、主城→金、宝箱→亮黄
- 脚手架→半透明棕

---

## 五、存档需求（问题 1 展开）

组件化后存档范围扩大，ISaveable 下沉到组件层：

| 存档对象 | 字段 | 说明 |
|---------|------|------|
| Building 核心 | type/coord/level/hp/faction/state/footprint/rotation | 所有建筑都存 |
| StorageComponent | storedAmount | 产出建筑必存 |
| ProducerComponent | 无需存档 | 每秒重算 |
| BuildingFactory 重建 | 根据存档 type 重新挂组件 + 恢复 StorageComponent.storedAmount | |

---

## 六、UI 栈系统（新增章节）

### 6.1 核心规则

- 所有弹窗打开时入栈，先进后出（栈结构）
- ESC 优先关闭栈顶最上层 UI
- 父子 UI 不嵌套栈，全部同一全局栈
- 空栈时 ESC 打开暂停窗口（暂停窗口也入栈）
- 暂停窗口在栈顶时 ESC 关闭它，栈重回空
- 关闭 UI 必须主动出栈，禁止 SetActive(false) 绕过管理器
- 模态弹窗锁定：栈顶 UI 生效时，底层 UI 不接收输入（可选扩展）

### 6.2 UIStack 层次

```
层0  普通游戏（无UI，自由操作）
层1  建造菜单（按B打开）
层2  建造模式（点建造按钮，菜单隐藏，虚影跟随鼠标）—— 无面板的虚拟UI层
层3  建筑面板（点击已有建筑打开）
```

建造模式作为虚拟 UI 层入栈，ESC 行为天然统一：
- 建造菜单(栈顶) → 点建造 → 菜单出栈隐藏 + 建造模式入栈
- ESC → 建造模式出栈 + 建造菜单重新入栈显示

### 6.3 接口约定

- `IUIStackEntry`：让有面板 UI 和无面板建造模式都能入栈
- UIManager 实现 Push/Pop/Peek

### 6.4 暂停菜单

已有，无需新增。空栈时 ESC 触发。

---

## 七、交互需求

### 7.1 问题 9 修复：InteractionManager 与 UI 点击冲突

方案A（推荐）：InteractionManager.Update 点击检测前加判断
```csharp
if (UIManager.Instance != null && UIManager.Instance.HasPanelOpen)
    return;  // 面板打开时不响应世界点击
```

### 7.2 问题 4：悬停高亮

- 每帧 Physics2D.OverlapPoint 检测鼠标位置
- 命中 IInteractable → SpriteRenderer.color 加亮
- 离开 → 取消高亮
- 点击后保持选中态，点空白取消
- 首版用 color 加亮，后期升级描边 Shader

### 7.3 问题 6：敌方建筑

- 点击敌方建筑 → 无响应（或浮动"敌方建筑"提示）
- InteractionManager 检查 `building.faction`，非 player 不走 Interact()
- 敌方建筑纯粹是战斗目标（3.4/3.5 处理）

### 7.4 拆除返还

按 HP 比例返还：满血 100%，半血 50%，以此类推。
建造中拆除返还 100%（按进度比例）。

---

## 八、主城设计（Kingdom 风格，新增章节）

### 8.1 设计理念

参考 Kingdom 游戏：主城不是功能建筑，而是**解锁建造系统的钥匙**。

### 8.2 游戏开局流程

```
游戏开始
  ↓ 地图中心预置【废弃城堡】（Building, state=Abandoned）
玩家点击废弃城堡 → 打开面板 → 显示"需要修复" + 修复造价
  ↓ 玩家有足够资源 → 触发修复
走 Constructing 进度（和建造统一）
  ↓ 修复完成 → state=Active
发 BuildingActivatedEvent
  ↓ BuildController 监听 → 解锁建造菜单
玩家可建造初始建筑（伐木场/采石场/农场/箭塔/城墙）
  ↓ 主城升级 → 分级解锁更多建筑
```

### 8.3 新增状态：Abandoned

- 废弃状态，可被修复但不产出/不可用
- 可交互（打开面板显示修复选项）
- 修复后转 Constructing → Active

### 8.4 科技树分级解锁

| 主城等级 | 解锁建筑 |
|---------|---------|
| 0（废弃） | 无（只能修复主城） |
| 1（修复完成） | 伐木场、采石场、农场、箭塔、城墙 |
| 2 | 市场（产金）、投石机 |
| 3 | 魔法塔、城门 |

### 8.5 主城本身

- 主城也是建筑，会被交互（打开面板）、会升级
- 升级走 Constructing 进度系统
- 主城被毁 = 游戏失败

---

## 九、后续 / 待定需求（首版不实现）

| # | 需求 | 说明 | 依赖 |
|---|------|------|------|
| 1 | 搬运 AI | NPC 工人轮询 IHarvestable，达阈值搬运到主城 | 3.10 移动系统 |
| 2 | 工人建造驱动 | 建造/升级/维修的工人驱动模式 | 3.10 移动系统 |
| 3 | 工人维修 | 工人消耗资源自动修理受损建筑 | 3.10 移动系统 |
| 4 | 种树 | 伐木场种植新树林资源点 | 无 |
| 5 | 炼金科技 | 木/粮转石/矿 | 科技系统 |
| 6 | 地图扩张 | 波次推进生成新资源点 | 3.7 波次系统 |
| 7 | 胜利条件 | 无限塔防，待定（沿用大纲） | - |
| 8 | OpenSubmenu | 建筑面板子菜单（侦察/攻击/标记） | 多系统 |
| 9 | 音频反馈 | 建造/产出/升级音效 | 音频系统 |
| 10 | 描边 Shader | 悬停高亮升级 | Shader |
| 11 | SpawnerComponent | 产兵 | 单位系统 |
| 12 | CombatComponent | 战斗 | 3.4/3.5 |
| 13 | RiftComponent | 裂隙刷怪 | 3.7 波次 |

---

## 十、实施优先级（MVP 闭环）

### P0（不做就转不起来）

1. 问题 9：修复 InteractionManager 与 UI 点击冲突
2. 问题 13：BuildController 入场景
3. 问题 12：建筑美术占位 prefab
4. UI 栈系统：UIManager Push/Pop + ESC 语义
5. Building 状态机：Placing/Constructing/Active/Dead/Abandoned
6. 建造进度系统：自动累计模式 + 骨架视觉
7. 组件化架构：IBuildingComponent + BuildingFactory 挂组件
8. ProducerComponent + StorageComponent：产能闭环
9. ProductionSystem：集中调度器
10. 资源点+工具模型：PlacementValidator 资源点校验 + 改造逻辑
11. 主城 Kingdom 流程：废弃城堡 → 修复 → 解锁建造
12. 多格占地 + R键旋转：二维 footprint + 校验

### P1（体验更好）

13. 问题 14：补充可建造建筑（伐木场/采石场/农场等）
14. 科技树分级解锁
15. 问题 4：悬停高亮
16. 建造手感：菜单隐藏/返回/吸附
17. 建造菜单 UI：图标+名称+造价

### P2（后续迭代）

18. 问题 1：ISaveable 存档（含组件存档）
19. 问题 6：敌方建筑提示
20. 问题 7：OpenSubmenu
21. 搬运/建造/维修的工人驱动（依赖 3.10）
22. 种树/炼金/地图扩张

---

> _本文为需求讨论成果汇总，审阅确认后决定如何并入 3.3.4 文档。_
