> ⚰️ **墓碑（2026-08-23 立）**
> 本文档已被 [改造计划/2_12_王国建筑系统迁移.md](../改造计划/2_12_王国建筑系统迁移.md)（步骤8.4 God-view 君主实体退役）与 [改造计划/2_3_空间与移动.md](../改造计划/2_3_空间与移动.md) 取代（RulerController 君主移动已随君主实体退役删除，67d3b67）。
> 内容仅作历史快照，**禁止作为实施依据**，引用请指向继任文档。

# 1.5 君主移动

> ✅ **已实现**（`Systems/Ruler/RulerController.cs`）。本文是初始移动逻辑设计；后续扩展见：
> - 存档系统改造清单 #7（ISaveable + RulerSaveData + SetRulerName）
> - 2.2 世界系统设计（ApplyInitialResourcesFromDifficulty 按难度设初始资源）
> - 2.4 销毁系统设计（ResetState 清引用归零资源）

### 优先级：🔴 最高

### 脚本和资源

| 文件 | 路径 | 说明 |
|------|------|------|
| `RulerController.cs` | `Assets/_Game/Systems/Ruler/` | 君主移动 + 输入处理 |
| `ruler_placeholder.png` | `Assets/_Game/Art/Sprites/` | 临时占位精灵 |

### 核心逻辑

```
RulerController : MonoBehaviour
  组件依赖：SpriteRenderer, Rigidbody2D

  字段：
    float moveSpeed = 5f
    float runSpeed = 8f
    bool isRunning = false（Shift 切换）

  Update() {
    读取 Horizontal 输入（A/D 或 ←/→）
    检测 Shift 按下 → isRunning = !isRunning
    float speed = isRunning ? runSpeed : moveSpeed
    rb.velocity = new Vector2(input * speed, rb.velocity.y)

    翻转 Sprite：
      if input > 0 → spriteRenderer.flipX = false（面朝右）
      if input < 0 → spriteRenderer.flipX = true（面朝左）
  }
```

### Rigidbody2D 设置

| 属性 | 值 |
|------|-----|
| Body Type | Dynamic |
| Gravity Scale | 0（横版不坠） |
| Constraints | Freeze Rotation Z ✅, Freeze Position Y ✅ |
| Collision Detection | Continuous |

### 实现步骤

1. 场景中创建空 GameObject "Ruler"
2. 添加 SpriteRenderer + Rigidbody2D + RulerController
3. 拖入临时占位 Sprite
4. 测试：运行后 A/D 左右移动，Shift 切换速度，面朝方向正确翻转

### 验证标准
- [ ] A/D 或 ←/→ 输入，君主左右移动
- [ ] Shift 切换奔跑/慢走，速度有明显差异
- [ ] 面朝方向随移动方向翻转
- [ ] 松开按键后立即停止（无滑行）

---

> _返回 [阶段1目录](./阶段1_目录.md)_
