# 2_17 AI 王国脑与自主成长 实施计划

> 日期：2026-08-23
> 状态：**P0 已完成（2026-08-27 收官）**· P1/P2 待实施（设计已声审定稿，D317~D350）
> 前置：**2_16 P0 必须已落地**——KingdomDef/TemplateLibrary/FoundingConfig 三 SO + KingdomRegistry（含玩家 id=0）+ FoundFirstGeneration（AI 实体已带 kingdomId 注册）；doc1 网格层（中区块坐标推导）；2_4 LOD 中区块（活跃带/热点接口）
> 目标：AI 王国从出生到军事期全程自主运转（P0/P1），sim 多王国化方案产出（P2）
> 边界：外交内容（⑪⑮宣战/姿态）归 2_18——本片⑪⑮仅占位可执行子集；灭亡处理归 2_19；情报面板 UI 归 2_13；染色渲染归 2_10
> 本计划承载 2_15 §3.5 预警的**最大工程改造**（per-kingdom 化逐文件拆解，见 §二）

---

## 〇 追记 · 2026-08-25 对账裁决回写（开工前基线）

> 本追记基于 2_16 落地后的接口对账，策划裁决已统一采准。**以下裁决全部覆盖/修订 §一、§二 对应原文**，实施以本追记为准。

**① 步骤1（实体归属标注）→ 降级并入 §零 前置检查**（2_16 P0 已完整前置落地：UnitFactory 两重载 `kingdomId=0` + controller.kingdomId；BuildingFactory.CreateBuildingInstance 带 kingdomId；SpawnFromSave 恢复 kingdomId；9 组合 canonical 回归已含玩家侧）。不再占实施序号，改为 §零 验收核对。

**双轨规则（步骤1 验收核对核心，直接写进 §零）**：
- `kingdomId` = **当前归属路由键**：派工/选中/敌我判定/王国脑池，一切路由用它。默认 0 门面不变。
- `originKingdomId` = **出身溯源**：仅两处消费——D295 性格混合、2_19 灭亡流民供给。**禁用于路由**。
- 流浪汉 `kingdomId=0` 的语义 = **“待招募人类”**。2_17 一切王国侧统计（人口台账/工人池）必须**双条件过滤（kingdomId + occupation/IsUnrecruitedVagrant）**，不得单看 kingdomId——否则流浪汉会被算进玩家人口。**此条进输入面约束**。

**② 步骤2 → 拆 2a / 2b；国库真源裁 B**：
- 2a：**KingdomState.resources 转正为 AI 国库真源**（per-kingdom 读 API：`CanAfford`/`Spend` 类）+ 入档回环验证（字段已在档 KingdomRegistry L136 写/L178 读，欠真链路回环断言，按冒烟纪律走真 `SaveManager.Load`）。
- 2b：仓储账本 per-kingdom 落地——若 WarehouseRegistry 需 per-kingdom，边界见下。
- **WarehouseRegistry 保持玩家物流专用**（2_12 仓库凑单 + 补丁 D 两道 kingdomId==0 过滤是玩家凑单链路守门员，per-kingdom 化=拆闸门=直接动玩家结算链，爆炸半径最大；AI 经济=台账制与 P0 人口台账同哲学；与 sim harness AbstractEconomySettler 抽象账本同源对齐，少一条差距账目；零触 RulerController/玩家结算双写红线）。A 方案（AI 仓储物理统一可劫掠）是 2_18+ 远期想象，不构成本片理由。

**③ 前置残留核点 + 两个补充裁定**：
- MonsterAI 被攻击信号源：挂 **BuildingAttackedEvent/伤害管线层**，不挂怪物目标选择层——P0 收窄放开与否不影响 AI 防守信号管线，两臂解耦。
- 补丁 D 与任务池边界：现状抑制=“AI 建筑不向玩家调度器发布”（过滤器形态）；**步骤3 per-kingdom 任务池落地后的正确终态=池隔离（AI 建筑发本国池，玩家调度器根本不订阅 AI 池）**。步骤3 验收项加一条：**补丁 D 过滤器收编进池隔离、不留永久双轨**——过渡期可留作 guard，步骤3 收口时凭证据裁去留。
- 前置残留核点（CellToMidChunk / LODSystem 活跃带 / DayCycleSettlement 挂点）→ 并入 §零 逐条验。
- ④ 读档纪律：**2_17 各步验收统一套“真 `SaveManager.Load` 全阶段链路”**（构造式/半链路不作读档回归通过依据）。

**输入面三约束（并入对应步骤）**：哨兵 -1 排除路由（武将/流浪汉不误领 AI 任务）/ 己方过滤边界（AI 国库独立账本，不与玩家凑单共用）/ task 抑制 + 步骤3 任务池**并案界定边界**。

**P0 首步 = 2a**（步骤1 已落地，AI「有源可花」为王国脑消费第一前置）。

### 追记二 · 2026-08-25 残核关账 + 2b 预研裁决（覆盖 §一/§二 对应原文）

**残核关账（§零 四条 PASS）**：CellToMidChunk ✅ 即用（GridSystem.cs:390）；LOD 活跃带块集公开查询缺口（`_activeBandSet` 私有，仅 ActiveBandCount）归**步骤13 后置补** `IsActivelyCovered(mid)`；DayCycleSettlement 前段可挂、CampUpgrader 已由 2_16 钉 L63；确定性纪律本卡零随机。

**2b 重定义（正文覆盖步骤2 原正文，原正文作废）**：步骤2a 已作（Spend 侧）。**步骤2b 新身子 = AI 实体产出入账路由（收入侧）**——据 2_15 §3.3 D281「可见即细模拟、账本同一本」，P0 恒 Fine 下 AI 国库收入 = AI 实体真实产出 → 路由入 KingdomState.resources。实施位随步骤3（任务池+实体化）自然后移，不再占"下一步"。

**⑤-3 入账路由裁 B（日结算搬运）**：在 DayCycleSettlement 五步之⑤（或 AI 段）每日把每 AI 建筑 StorageComponent 累计产出 `AddResources` 入 KingdomState.resources 并清零本地仓储；复用 IsFull 容量语义（预见 ProducerComponent:129-134 需工人、落点 StorageComponent:140-162）。**两条硬性**：a) 建筑遍历**固定排序**（禁依赖注册顺序/FindObjectsOfType 序），同 seed 逐字节一致须覆盖 AI 段；b) **差距账本登记**：sim 瞬时入账无 Storage 中介 vs Unity 日结两段式，登记 15_差距账本，与 AbstractEconomySettler（步骤14 镜像 sim 公式）对账时点标注——不落不算收口。**水井裁代码拦截**（否决 def 规避）：AI 建筑不充玩家 WaterNetwork（kingdomId 守卫，P0 四补丁同模式），防御王国脑建造效用/模板池/动态立国 castle 复用绕过配置；def 侧顺手不做为修复本体。

**① 实体化钉步骤3**：Foundry 直接直出实体工人（台账+实体**双写=步骤3 过渡态**）。**真源演进规则**：步骤4 人口系统 per-kingdom 时，workerCount/warriorCount 台账**转派生统计（实体=唯一真源）**，防双真源漂移（读档双份卡同族教训）。

**② Faction 两阶段**：步骤3 补单位 kingdomId 守卫（SelectionController 选中 + PopulationSystem 人口台账双条件过滤）；步骤4~5 Faction 完整收编。**sim-sync 义务**：Faction 枚举改动动 AI.Core/Ports/Faction.cs（双份拷贝区）——先报、同步训练仓镜像、登记 15_差距账本，按 sim-sync 纪律，不可静默改。

**⑤-4 步骤3 验收扩容（5 项）**：a) 池隔离生效（A 国工人不领 B 国任务）；b) 实体工人既出且台账双写；c) 单位守卫两处（SelectionController + PopulationSystem）生效；d) AI 建筑产出恢复（HasWorkerAssigned 链通）；e) 水井拦截。**补丁 D 收编**：池隔离为主体，原过滤器暂留 guard，**步骤3 冒烟取证后裁去留**（不因池隔离一步到位就顺手删，证据先行）。**§二 文件清单**：删 WarehouseRegistry per-kingdom「Dictionary 化」行；增入账路由（DayCycleSettlement AI 段/产出入账 Route）、步骤4 台账派生改造。

**2b 落地（2026-08-26，冒烟 ALL PASS = 收口）**：新增 `AIEconomySettlement` 静态类（Systems/Kingdom/AIEconomySettlement.cs），`Tick()` 遍历 KingdomRegistry 非玩家王国（kingdomId>0，IsPlayer 直接跳过=玩家零回归）→ `QueryKingdomBuildings` 收集本王国 IsActive 建筑 → **固定排序**（主键 coord.y→coord.x，次键 def.id String.CompareOrdinal，彻底弃注册序/FindObjects 序＝硬性 a）→ 七类 Storage 按**五经济资源白名单**（Gold/Stone/Wood/Food/Metal 入包；Ore/Crystal/FireOil/SpecialFood/Meat/弹药跳过保留）→ `kingdom.AddResources(pack)` 入国库 → `storage.Take(storedAmount)` 清零并触发 OnStorageChanged（IWarehouse 有 Take，修 CS0070 不可从类外 Invoke event）。DayCycleSettlement 新增第 6 段调用（交易额度冷却后、牧场/营地区段前），不扰②残核一侧的⑤步权威序，CampUpgrader 仍在⑤。水井天然不路由（W 类 Production 归 ProducerComponent，且步骤3 kingdomId>0 拦截 Player.WaterNetwork 已落，冒烟断言 AI 水井不产水）。15_差距账本「一·补二 AI 经济入账语义差异」登记（sim 瞬时入账无 Storage 中介 vs Unity 日结两段式，1 日滞后对账标注、步骤14 镜像 sim 公式时处理）。冒烟 `Valley2_17_Smoke_2b.cs`（Play，SEED=20260826，两轮）四项全绿＝收口门槛：settleOK 入账+清零 / playerZero 玩家零回归 / wellOK AI 水井不产水 / **确定性两轮逐字节一致（ClearAmbient 真销毁非受控 AI 建筑消时间方差 + 锚定受控仓库基线 100→kStone=125，活世界方法债清偿）**。
- **① A 准**：步骤4 派生统计纯 kingdomId（分桶键），无 Faction 依赖、无 sim-sync 义务，本步不动 AI.Core/Ports/Faction.cs。真源演进生效：workerCount/warriorCount 台账转派生（实体=唯一真源）——Foundry 不再手写台账，KingdomState 改属性对实体按 kingdomId 派生；玩家=桶0（玩家指标零回归），流浪汉双条件过滤不误计。
- **② Faction 收编 重钉 5→步骤10**（消费驱动）：步骤5~9 全是 per-kingdom 系统改造（训练队列/领土/指令/脑/效用评分均 kingdomId 分桶），零新增阵营面判定→延后收编零成本；第一个真实消费者=步骤10 军事效用项⑦（招战士入场、MonsterAI 放开窗口「AI 防御就绪」）——敌我识别/寻敌/被攻击响应全在战斗面。收编后既有 kingdomId 守卫（选中/水井/物流/怪目标）迁移为 faction 判定+kingdomId 路由双层，一次性收编。**步骤10 到位时先报 Faction sim-sync 方案（枚举追加+训练仓镜像+15_账本登记项）再动**。
- **③ 新增指令 patch C（随步骤4 落）**：补单位级盲区——MonsterController.FindNearestHuman 感知过滤 GetFaction()!=Human_Player 会把步骤3 实体化 AI 工人（Human_Player+kingdomId>0 冒充态）当玩家目标，P0 只袭玩家意图被违（建筑受 L192 保护、工人裸奔）。修复：FindNearestHuman 加 kingdomId==0 守卫（与 L192 同模式同语义）；此守卫步骤10 Faction 收编时随迁移退役（与既有守卫同生命周期）。
**步骤4 验收面（含③）**：台账转派生统计（实体唯一真源，worker/warrior 分国计数、AliveWorkerCount 按国）/ 流浪汉双条件过滤不误计 / FindNearestHuman 守卫落位+冒烟（AI 工人不入怪感知集）/ 确定性同 seed 一致 / 玩家人口指标零回归（ThroneAnchor GameOver 驱动仅桶0，防 AI 工人虚增保命）。

**步骤4 关账扫描：单位级 Human_Player 过滤面分类表（一表两用=步骤10 Faction 收编迁移清单）**
（全库 grep Faction.Human_Player 48 处 / GetFaction() 36 处，仅列单位级消费面）
- **① 已守卫（退役为迁移）**：MonsterController.FindNearestHuman(patch C,kingdomId==0) / SelectionController L152/L182 / PopulationSystem.IsPopulationEntity(step3) / ThroneAnchor.HasRemainingWorker+AliveWorkerCount(step4)。
- **② 裸奔→本次已补守卫(live bug，同模式 kingdomId==0)**：HappinessSystem.OnNewDay(AI工人稀释玩家幸福) / SatietySystem.GetAverageSatiety+OnNewDay(AI工人吃玩家国库粮/拉低均饱食) / TrainingSystem 转职池构建+GetTrainableCount(AI工人入玩家转职池/可训练数)。
- **③ 桶语义天然安全（说明为何安全）**：PatrolTaskSystem~189(AI工人无 NPCBrain→被跳过) / SiegeProductionSystem~78(仅统计机器职业,AI非机器) / RulerController~196/244(仅 Ruler 职业判王) / TrainingSystem CanTrainGeneral(仅 General 职业) / GridSystem~332(仅 Undead) / 战斗敌我识别族(UnitController~954/996/1077、Building~215/247/279、BuildingComponents~118、DamageSystem~327、GroundEffect~96/111/135、Projectile~196、NPCBrain~325/454/1246/1461、Perception~55/85)：按 faction 判敌我，AI(HP 冒充)被当作玩家友军=与冒充态一致，不污染玩家指标，收编后自动正确 / 建筑·UI·spawn·显示面(Building~519、UnitFactory~136、VagrantCamp~251、InteractionManager 及系列 Interactor/BuildingMenuPanel/AIDebugUI/FormationPanel)：非单位玩家指标面。
- **④ 步骤10 退役范围**：①+② 全部 kingdomId==0 守卫一次性迁移为 faction 判定+kingdomId 路由双层，逐行核退役，禁止零头遗漏。
- **入残⚪批次**：步骤4 LoadState 存档改派生字段的存读回环断言（存→读→派生计数=存前），随步骤3 残⚪四项同批完整局 Play 一次清。

**步骤6 TerritorySystem 落地追记（2026-08-25）**：
- 注入点**实际用 LoadManager.EnterPlaying**（偏差已报）：GenerateMap 末尾在读档路径建筑未恢复会空账本（D342 读档从建筑重推需建筑就位后），EnterPlaying 是新游戏（InitializeWorld 完成）与读档（_saveLoader.Load 恢复建筑）两路的共同就绪点——既覆盖玩家+AI（①意图），又让读档重推成立（④⑤）。
- 初始圈入 Chebyshev 3×3（②）；D343 口径每国 patch 数列见验收。渲染归 2_10（本步只出 GetAllTerritory 数据）。
- 冒烟（seed=20260825 两轮逐字节一致）：total=47 neg=0 k0/k1/k2/k3=9/14/12/12 disjoint=True（Small 图两国不相交的泛化断言）rebuildSame=True（⑤ 读档两路重推=新建一致）。
- **§④债（本步登记）**：TerritorySystem.RebuildInitial 现两路（新游戏/读档）都跑（P0 领土无存档、读档从建筑重推=正确）。**步骤8 或步骤12（先到者）落地领土入档/扩张时，RebuildInitial 必须加 foundKingdoms 门控**（新游戏走 RebuildInitial、读档走存档恢复），与 FoundFirstGeneration 门控（2_16 根因二）同族并列登记，防读档把扩张后领土重置回初始圈入。

**步骤7 指令通道建造门面 落地追记（2026-08-26）**：
- **招募半边**步骤5 已随 TryTrain 完成（per-kingdom 队列 + CanPayRecruit/PayRecruit 分国）。本步补**建造半边**：`BuildController.TryBuild(def, sub, orient, kingdomId=0)` 公共门面——选址校验/扣费/落成同一套；`PlacementValidator.ValidatePlacement` 加 kingdomId（资源门玩家走 RulerController、AI 走 KingdomState.CanAfford）；`CanPayBuild/PayBuild` 分国（玩家→WarehouseHelper 多仓凑单；AI→KingdomState.Spend 台账制无事件）。玩家原 TryPlace 鼠标路径改路由 TryBuild(0)（零回归）。
- **落成**：`b.kingdomId=kingdomId`（AI 建造归属该国）；isPlayerBuilt=true（与玩家同 Constructing 链）。
- **领内口径**：本步沿用既有选址规则（D345：前置/资源走同玩家一套）；**领土入界强制归步骤12**——现 TerritorySystem 只记初始圈入不推进，提前强制领内会锁死玩家（扩张落地步骤12 再收口，见步骤6 追记 §④债）。
- **无 sim 债务**：本步为 Unity 侧指令门面，不动 AI.Core/Ports/factor_registry，无 sim-sync 义务，0 新增训练仓账目。
- **冒烟 `Valley2_17_Smoke_7`（Menu「Valley/验证/2_17_步骤7_指令通道建造门面」，GameScene Play 上下文，SEED=20260826 两轮）五项全绿**：AI建造归属+扣费（aiId=1，wood 10→6）/占用拒绝（同格拒且不再扣）/资源不足拒绝（国库清零拒、无落成）/玩家零回归（建筑 kingdomId 恒 0）/确定性两轮逐字节一致=OK。⚠️ 裸 MainMenu Play 无世界引导致 noAI（环境非逻辑）——须切 GameScene 驱动真实世界再跑。



---

## 零、前置检查（不满足则停）

- [ ] 2_16 P0 已落地：`KingdomRegistry` 可用（含玩家 id=0）、FoundFirstGeneration 能产出带 kingdomId 的 AI 建筑/人口
- [ ] doc1 中区块坐标推导可用（`CellToMidChunk`，整数除法纯函数）
- [ ] 2_4 LODSystem 活跃带区块集 + 战斗热点列表可查询（D344/D333 信号源）
- [ ] DayCycleSettlement 日结算入口可挂载前段逻辑
- [ ] 全程确定性纪律：新代码禁 `UnityEngine.Random`，统一注入 `System.Random(seed)`

---

## 一、实施步骤（按依赖序，每步可独立验收）

### P0：归属地基 + 王国脑骨架（可玩的单 AI 王国）

#### 步骤 1：实体归属标注

**文件**：`Systems/Unit/UnitFactory.cs`、`Systems/Building/BuildingFactory.cs`（或实体基类）

- 实体类（Unit/Building）加 `int kingdomId` 字段；两 Factory 生成 API 加 `kingdomId` 参数（默认 0）
- 玩家侧既有调用不传参 → 走 id=0，零改动（D329 门面原则）

**验收**：编译 0 警告 0 错误；同 seed 玩家侧行为逐字节不变（冒烟 #11）。

#### 步骤 2：仓储账本 per-kingdom

**文件**：`Systems/Kingdom/WarehouseRegistry.cs`、`IWarehouse.cs`

- 内部状态 `Dictionary<kingdomId, 账本>`；存取 API 加 kingdomId（默认 0）
- 2_16 FoundFirstGeneration 的 baseStockpile 预置写入对应国账本

**验收**：两国账本互不串（写入 A 国读 B 国为空）；玩家仓储行为不变。

#### 步骤 3：任务池 per-kingdom 路由

**文件**：`Systems/AI/TaskScheduling/TaskScheduler.cs`

- 任务池按 kingdomId 分桶；派工仲裁只在同桶内匹配（工人只领本国任务，D330）
- 日 tick 顺序接入 D347 五步权威顺序的第 5 步（既有位置不动）

**验收**：A 国工人不会去完成 B 国建造任务；玩家派工行为不变。

> **步骤3 落地（2026-08-25，ac1ffbc）**：任务池按 kingdomId 路由 + 实体化双写（①钉步骤3）+ 单位守卫两处 + 水井拦截 + AI 源收编。文件：TaskScheduler(移除 Awake AI建筑整块跳过 guard→收编进池隔离, idleKingdom==tKingdom + SourceKingdom) / KingdomFoundry(SpawnAiWorkers 台账+实体双写,index均布不耗rng) / PopulationSystem+SelectionController(kingdomId 双条件守卫) / ProducerComponent(水井 kingdomId>0 拦截)。确定性冒烟(seed=20260825 Medium/Easy 两轮逐字节一致): ledger==spawned k1/k2/k3各=4、IsPopulationEntity(AI工人)=false、aiBld=9。guard 去留: TaskScheduler Awake 整块跳过=去(池隔离完全取代); 双条件守卫/水井拦截/WarehouseRegistry 玩家物流隔离=留(终裁在策划)。
> **残⚪四项去向**：行为级①池隔离(A国工人永不领B国任务)/④AI 产出恢复(HasWorkerAssigned 链)/⑤水井供水/③双守卫运行时——本步为**结构前提已具备**（编译+源码守卫+确定性冒烟），完整局行为终验收时点=**步骤 8/9 后首个正常开局 Play 会话**（王国脑日 tick 运行时，届时补打回填）。

#### 步骤 4：人口系统 per-kingdom

**文件**：`Systems/Kingdom/PopulationSystem.cs`

- 人口登记/统计 per-kingdom；`AliveWorkerCount`（D279 灭亡判定数据源）按国统计
- 流浪汉营地数据保持全局（2_16 Camp 语义，不属任何国）

**验收**：AliveWorkerCount 按国正确；灭亡判定挂点数据可用（2_19 消费）。

#### 步骤 5：训练队列 per-kingdom

**文件**：`Systems/Building/TrainingSystem.cs`

- 训练队列按 kingdomId 分桶；招募 API 加 kingdomId（默认 0）——此即指令通道"招募入口"的一半（D345）

**验收**：两国训练互不干扰；玩家招募不变。

#### 步骤 6：TerritorySystem 只读账本 + 染色事件

**文件**：新增 `Systems/Kingdom/TerritorySystem.cs`；改 `Systems/Kingdom/KingdomManager.cs`（KingdomState 扩展）

- 账本 `Dictionary<Vector2Int 中区块, int kingdomId>` 稀疏存储（D325/D342 唯一真源）
- 初始领土写入：FoundFirstGeneration 末尾按 **D343 口径**（初始建筑外扩 1 中区块；动态=营地中区块+1 圈）圈入
- `TerritoryChangedEvent`（EventBus）+ KingdomState.领土句柄（只读视图）
- 染色数据接口供 2_10 overlay（渲染归 2_10，本步只出数据）

**验收**：开局染色可见各国色块+玩家；领土数符合口径（直径 3~5 中区块）；Small 图两国不相交（冒烟 #16）。

#### 步骤 7：指令通道最小集

**文件**：建造入口（2_12 建造系统）+ TrainingSystem 招募 API

- 建造入口加 `kingdomId`（默认 0）：选址合法性校验（领内/前置/资源）走同一套
- 至此 D345 通道最小集就位：建造+招募

**验收**：AI 经通道下指令与玩家手工等效（同资源扣减、同合法性规则）。

#### 步骤 8：KingdomBrain 骨架 + 日 tick 挂点

**文件**：新增 `Systems/AI/KingdomBrain/`（KingdomBrain.cs / ScriptStageMachine.cs / FocusController.cs）；改 `DayCycleSettlement.cs`

- DayCycleSettlement 前段接入 **D347 五步权威顺序**：①SimMode 判定（P0 恒 Fine 占位）②Brain.Tick ③领土变更（P0 空）④CampUpgrader（2_16 已有，顺序归位）⑤既有日结算
- 剧本状态机：四阶段+阈值表+最小停留+每日最多升一级（D317~D320/D349，阈值入 KingdomBrainConfig）
- 常设底线（粮/被攻击）+ 焦点输出（防抖 ≥3 日）+ 事件打断框架（EventBus 订阅，未落地源接口占位，D340）
- KingdomBrain 生命周期：Foundry 创建 → D279 灭亡销毁+退订（D337；灭亡触发本体归 2_19，本步留销毁钩子）

**验收**：注入资源/人口满足阈值 → 剧本按最小停留推进、单日最多升一级（冒烟 #1）；玩家 id=0 无 Brain（冒烟 #13）。
- **§④债（TerritorySystem 门控，登记自步骤6）**：步骤6 已实现 RebuildInitial 读档/新建两路重推。**领土入档/扩张落地时（步骤8 或步骤12，先到者）**，RebuildInitial 加 foundKingdoms 门控——新游戏走 RebuildInitial、读档走存档恢复（领土账本 = 存档值），与 FoundFirstGeneration 门控（2_16 根因二）同族，防读档重置扩张后领土。

#### 步骤 9：效用评分器（P0 子集 8 项）

**文件**：新增 `Systems/AI/KingdomBrain/UtilityScorer.cs` + `Data/UtilityActionConfig.cs`（SO）

- 行动子集 **①~⑥+⑬⑭**（D345）；阶段可见性门控 + 二值可行性（D346）+ 四因子评分（D323）
- 需求强度缺口函数逐项实现（住房/仓储/产能/采集/粮/人口/重建/防御）
- 焦点执行下发 → 指令通道（建造/招募）→ L2 派工

**验收**：P0 阶段 AI 可执行 8 项且存活期不卡死（⑥招工人可执行，冒烟 #19）；常设底线触发（冒烟 #4）；性格分化可测（冒烟 #3）。

**P0 整体验收**：单 AI 王国从出生自主成长至扩张期（不夭折、无 ≥10 日停滞）；同 seed 逐字节一致；玩家侧全流程无回归。

### P1：全效用 + 领土推进 + 粒度切换

#### 步骤 10：效用补全 15 项

**文件**：UtilityScorer 扩展 + UtilityActionConfig 补条目

- 补 ⑦招战士（接 D348 兵力目标公式：`clamp(2+⌈威胁值×3⌉+阶段系数, 2, 2+工人数)`，威胁值分母 max(己方,1)）、⑧科技升级（挂 CastleUnlockTable，解锁态 per-kingdom 步骤 11 前置）、⑨修工事城墙、⑫边境增援（L3 意图接口）
- ⑪出征军/⑮外交姿态：**占位可执行子集**（评分/焦点可运转，宣战动作接口指向 2_18 未落地桩）

**验收**：兵力目标随威胁上调（冒烟 #5）；⑦⑧⑨⑫可被选为焦点并执行。

#### 步骤 11：per-kingdom 第二步（全量归属 9 系统）+ 指令通道补全

**文件**：Happiness / Satiety / Tax / Trade / Ranch / SiegeProduction / DayCycleSettlement / CastleUnlockTable / WaterNetwork（门面+内部字典，逐个改造逐个冒烟）

- 科技/推边界指令入口加 kingdomId（D331 全量）
- CastleUnlockTable 解锁态 per-kingdom（动态立国科技不继承的运行时载体）

**验收**：每改一个系统跑一次"同 seed 玩家侧逐字节一致"；两国科技解锁互不影响。

#### 步骤 12：领土推进 + 玩家建造纳土 + 吞并接线

**文件**：TerritorySystem 扩展（推进入口）；建造落成回调接线

- AI 推进：⑩被选中 → 邻接无主中区块 1~2 块（可走率 ≥50%）、冷却 5 日、同日竞争 kingdomId 顺序（D326）；扩张额度 clamp(4+工人−非初始占区, 0, 96)（D327/D341）
- 玩家建造纳土：邻接无主地建筑落成 → 自动纳入（D327）
- CampUpgrader 接 TerritoryChangedEvent → 吞并（D306 联动，五步顺序第 4 步）

**验收**：冒烟 #6/#7/#8/#15/#18 全绿。

#### 步骤 13：SimModeManager

**文件**：新增 `Systems/Kingdom/SimModeManager.cs`；LODSystem 出活跃带区块集查询

- 判定：活跃带覆盖→Fine（立即）；连续 2 日未覆盖→Abstract（迟滞）；战斗锁强制 Fine（D333/D344）
- 休眠/唤醒：NPCBrain Think 开关 + 寻路停启（实体常驻，D334）
- 存档记录 SimMode

**验收**：缩放全图 Fine 集合不变（冒烟 #17）；出视野 2 日切抽象、入视野原地续干、反复切 10 次账本无差（冒烟 #9/#10）。

#### 步骤 14：AbstractEconomySettler + 对账

**文件**：新增 `Systems/AI/KingdomBrain/AbstractEconomySettler.cs`（**纯 C# 放 AI.Core 同源目录结构，零 Unity 引用**，D336）；SimModeManager 对账逻辑

- 每抽象国日 tick：人口×生产率→资源→推进建造/招募队列（公式与 SimEconomy 同构，抽取共享公式或镜像实现）
- 对账：人口差值补删（unit id 序，新兵刷住宅、无住宅刷王座，D335）；队列进度续接

**验收**：抽象国资源/队列日推进正确；切回细模拟数据连续无跳变；同 seed 确定性（冒烟 #9/#12）。

**P1 整体验收**：≥2 AI 王国全程自主运转至军事期（2_15 §七.5 口径：不因自身决策夭折、无 ≥10 日停滞）；抽象/细切换无跳变；性能预算 ≤ 现单王国 ×2。

### P2：训练前置 + 调参 + 联调（本片只出方案与联调，不动 champion）

#### 步骤 15：rubber-band 三层系数落地

**文件**：`Data/AIKingdomBalanceConfig.cs`（SO）+ AbstractEconomySettler / UtilityScorer / TerritorySystem 消费点

- 三层：①抽象结算产出系数 ②需求强度缩放 ③领土推进冷却系数（D350）；幅度上限克制（D288）

**验收**：落后国轻加速/领先国轻限速可观测（对比组数据）；玩家无"作弊感"（幅度上限内）。

#### 步骤 16：情报面板数据联调

**文件**：KingdomState 查询接口（剧本阶段/焦点/兵力目标/领土数）→ 2_13 面板（UI 归 2_13）

**验收**：面板数据与内部状态一致；玩家无剧本阶段项（D338）。

#### 步骤 17：阶段 B sim 多王国化方案（文档产出）

**产出**：sim 侧改造方案（SimWorld 多王国账本/领土/SimMode 抽象层/外交状态）+ 任务卡草案——提交训练师工作区走提案流程（改 harness 须先 commit→改→双门禁，AGENTS 纪律）；效用权重+兵力目标系数入 factor_registry 的注册表草案

**验收**：方案文档过 requirement-review；不阻塞本片 P0/P1 玩法。

---

## 二、涉及文件清单（per-kingdom 逐文件拆解，D329/D330）

### 第一步：归属地基（P0，6 系统）

| 文件 | 改动 |
|------|------|
| `Systems/Unit/UnitFactory.cs` + Unit 实体 | kingdomId 字段+参数 |
| `Systems/Building/BuildingFactory.cs` + Building 实体 | 同上 |
| `Systems/Kingdom/WarehouseRegistry.cs` / `IWarehouse.cs` | 账本 Dictionary 化 |
| `Systems/AI/TaskScheduling/TaskScheduler.cs` | 任务池分桶路由 |
| `Systems/Kingdom/PopulationSystem.cs` | 人口 per-kingdom + AliveWorkerCount 按国 |
| `Systems/Building/TrainingSystem.cs` | 队列分桶 + 招募 API 加 kingdomId |

### 第二步：全量归属（P1，9 系统，逐个改造逐个冒烟）

| 文件 | 改动 |
|------|------|
| `Systems/Kingdom/HappinessSystem.cs` | 内部态 Dictionary 化 |
| `Systems/Kingdom/SatietySystem.cs` | 同上 |
| `Systems/Kingdom/TaxSystem.cs` | 同上 |
| `Systems/Kingdom/TradeSystem.cs` | 同上 |
| `Systems/Kingdom/RanchSystem.cs` | 同上 |
| `Systems/Kingdom/SiegeProductionSystem.cs` | 同上 |
| `Systems/Kingdom/DayCycleSettlement.cs` | 结算 per-kingdom + 五步顺序接入（步骤 8 已挂点） |
| `Systems/Kingdom/CastleUnlockTable.cs` | 解锁态 per-kingdom |
| `Systems/Kingdom/WaterNetwork.cs` | 按领土归属 |

### 第三步+新建（指令通道与新系统）

| 文件 | 改动 |
|------|------|
| 2_12 建造入口 | 加 kingdomId（默认 0） |
| `Systems/Kingdom/TerritorySystem.cs` | **新增**：账本真源/推进/纳土/事件 |
| `Systems/AI/KingdomBrain/KingdomBrain.cs` | **新增**：日 tick 主脑 |
| `Systems/AI/KingdomBrain/ScriptStageMachine.cs` | **新增**：剧本状态机 |
| `Systems/AI/KingdomBrain/FocusController.cs` | **新增**：焦点+常设底线+打断 |
| `Systems/AI/KingdomBrain/UtilityScorer.cs` | **新增**：15 项评分 |
| `Systems/AI/KingdomBrain/AbstractEconomySettler.cs` | **新增**：纯 C# 抽象结算 |
| `Systems/Kingdom/SimModeManager.cs` | **新增**：粒度切换+休眠唤醒+对账 |
| `Systems/Kingdom/KingdomManager.cs` | KingdomState 扩展字段 |
| AI.Core（Unity 侧）↔ harness Core（sim 侧） | AbstractEconomySettler 公式同构同步（双份拷贝纪律） |

---

## 三、SO 配置（6 个，全部占位可调）

### KingdomBrainConfig（`Resources/Config/Kingdoms/`）

| 字段 | 默认（占位） | 依据 |
|------|------|------|
| surviveMinDays / developMinDays / expandMinDays | 2 / 3 / 3 | D317 最小停留 |
| thresholdSurviveToDevelop（人均有房/粮储日/失业） | true / 3 / 0 | D317 |
| thresholdDevelopToExpand（工人/产能/净流入日） | 8 / 3 / 3 | D317 |
| thresholdExpandToMilitary（战士/人口/扩张占区） | 4 / 12 / 2 | D317/D349 |
| maxStageUpPerDay | 1 | D319 |
| focusMinDurationDays | 3 | D322 防抖 |
| grainReserveDaysFloor | 2 | D322 常设底线 |

### UtilityActionConfig

15 条行动定义：`stageVisibility[] / personalityAxis / needFnParams / feasibilityRules / stageWeight[]`（阶段A 手配落点，D323/D346）。

### MilitaryGoalConfig

| 字段 | 默认（占位） | 依据 |
|------|------|------|
| baseSoldiers | 2 | D348 |
| threatCoefficient | 3（⌈威胁值×3⌉） | D348 |
| stageBonus[]{存活,发育,扩张,军事} | {0,0,1,2} | D348 |
| threatDenominatorFloor | 1（max(己方兵力,1)） | D339/D348 |

### TerritoryConfig

| 字段 | 默认（占位） | 依据 |
|------|------|------|
| expandBatchMin/Max | 1 / 2 中区块 | D326 |
| expandCooldownDays | 5 | D326 |
| walkableRatioGate | 0.5 | D326 |
| expansionQuotaBase | 4（4+工人−非初始占区） | D327/D341 |
| territoryHardCap | 96 中区块 | D341 |
| initialTerritoryMode | 建筑外扩 1 中区块 / 营地+1 圈 | D343 |

### SimModeConfig

| 字段 | 默认（占位） | 依据 |
|------|------|------|
| offscreenDaysToAbstract | 2 | D333 |
| combatHotspotForceFine | true | D333 |
| （视野=D77 活跃带，无额外参数——信号源 LODSystem） | — | D344 |

### AIKingdomBalanceConfig

三层系数（D350）：`abstractOutputMultiplier / needIntensityScale / expandCooldownScale`，各带 {落后加速, 领先限速} 两档幅度+上限（占位 ±10%，D288 克制）。

---

## 四、冒烟验证 checklist（对照 2_17 §六 19 条）

- [ ] P0：#1 剧本推进 / #2 单向不回退 / #3 性格分化（好战⑦+⑪ ≥ 经济国×2）/ #4 常设底线 / #11 per-kingdom 回归 / #12 确定性 / #13 玩家无剧本 / #19 P0 行动子集
- [ ] P1：#5 兵力目标 / #6 领土推进+额度 / #7 建造纳土 / #8 吞并联动 / #9 SimMode 反复切 / #10 战斗锁 / #14 脑生命周期退订 / #15 领土真源 / #16 初始口径不重叠 / #17 缩放不变 Fine 集 / #18 日 tick 顺序
- [ ] 每改一个经营系统（步骤 11）：同 seed 玩家侧逐字节一致
- [ ] P1 末：≥2 AI 王国 30 日自主运转录屏留证

## 五、验收标准

1. P0/P1/P2 步骤各自验收全绿（§一）
2. 2_17 §六 19 条冒烟全绿
3. 性能：视野内细模拟 ≤ 现单王国 ×2（活跃带 300~400 单位口径）；抽象结算 N≤7 可忽略
4. 同 seed 全程（含抽象路径）逐字节一致
5. 全部新增可调数值落 6 SO（本计划 §三），无硬编码魔法数

## 六、风险与回滚

| 风险 | 对策 |
|------|------|
| per-kingdom 改造破坏玩家侧 | 门面模式零调用点改动（D329）+ 每步"同 seed 逐字节一致"冒烟；单系统单提交，可独立回滚 |
| AI 卡死存活期/停滞 | P0 子集必含 ⑥招工人+⑬⑭（D345）；剧本保底+常设底线兜底 |
| 抽象/细对账跳变 | 常驻实体+差值确定性补删（D334/D335）；反复切换专项冒烟 #9 |
| 性能超预算 | "视野"=活跃带而非视口（D344）；预算表逐项验收 |
| 同构公式双份漂移 | AbstractEconomySettler 公式改动走 AI.Core↔harness 双侧同步提交纪律 |
| 2_18 未落地导致⑪⑮悬空 | 占位可执行子集（评分可运转、宣战接口桩），2_18 落地后接真实现 |
| 与 2_16 依赖断裂 | 零、前置检查卡点：Registry/FoundFirstGeneration 未落地则停 |

> 本片 P0 完成即可与 2_16 联调出"出生即自主成长"的可玩切片；P1 完成进入 2_18（多王国外交与战争，⑪⑮ 真实现接此）。

---

## 七、P0 收官记录（2026-08-27 · ⚙️ 状态翻 P0✅）

**P0 完整局验收批收官成立**（HH.27 独立批，确定性状态机验收，全证据见 `多Agent交接/HH.27_P0完整局验收批执行报告_待策划裁决.md` 终版）：

- **A3 确定性 = 绿**：同 seed 两纯轮逐字节一致。根因实锤为 **widow-window（复位窗口期污染打 GameOver）**——harness 贴实时会话触发时复位链清工人后让出一帧，旧 ThroneAnchor 仍 Playing 轮询 → `IsKingdomLost=true` → `SetState(GameOver)` → 非法跳过 `surviveMinDays=2` 门控（ScriptStageMachine L83）→ day1 即 Develop。**GO 三修复（均 harness-only，产品零改动）**：复位链头 `SetState(MainMenu)` 关窗 + `DisarmDisasters` 前置 `InitializeNewGame` + ResetState 后反射清零 `_dayTimer` → r1/r2/r3 post-Init 全 Playing、时间线 `SDDDDD…` 对齐、`u=22/22/22` 非退化。
- **B3 定性 = harness 时序假差（非产品读档缺陷）**：存读点插在 simDay=25 日结算前 → 日结算 ±1 错位 + 在途储仓丢失。产品 LoadState 语义自洽（RD2-② v2 门控 OK）。**带红做独立回归卡挂 2_11**。
- **B1/B4/B5 = 流浪汉池空（pump 环境断层）**：纯 pump 无地图流浪汉 → 招工人→成长段判 N/A，**归人工 Play 黄旗三条**。
- **绿色项**：A3 / A4 玩家零回归 / B2 供水抽象产出 / B1 正向招募通道 pTrue / RD2-①轮间清点 / RD2-②存读 v2 门控。
- **登记债**：④债 Ruinit 门控归步骤12；【确定性全库扫描】小卡（22 个 UnityEngine.Random 消费面，TrainingSystem L305 与 VagrantCampSystem 已修）。
- **数据卫生**：全部存档槽位 isFinished=false 零误标、smoke 槽位无残留。

**P0 章节翻页。下一站：P1 步骤10（效用补全 + Faction 收编 sim-sync 报备）**——动 Faction 枚举前置报 sim-sync 方案（枚举追加+训练仓镜像+15_账本登记项）后再动。




