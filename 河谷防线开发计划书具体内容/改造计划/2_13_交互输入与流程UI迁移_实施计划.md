# 2_13 交互输入与流程 UI 迁移 实施计划

> 日期：2026-08-14
> 状态：待实施（设计稿 `2_13_交互输入与流程UI迁移.md` 已定稿）
> 更新：2026-08-14 终审同步——IssueRightClick 补 Follow 分支（D2，步骤 3/11）、设置面板（D240）/倍速入口（D241）本篇实现不再推 2_10（D256，步骤 5）、新增步骤 11B D118 信息面板/11C P1 输入档（D274）、验收与设计稿 §六对齐（D117 已移交 2_12，D256）（2026-08-14 终审修订，D2XX）
> 前置：[2_3_空间与移动](./2_3_空间与移动.md)（PathFollower 寻路/移动）、[2_8_AI应用层](./2_8_AI应用层.md)（任务/编队/守卫部署指令消费）、[2_2_建筑与占格](./2_2_建筑与占格.md)（Build 模式放置交互）、[2_10_渲染与摄像机](./2_10_渲染与摄像机.md)（CameraRig pan/zoom/2D 正交射线）、[2_11_存档与迁移](./2_11_存档与迁移.md)（kingdomName 存档 schema、SaveSlot 摘要显示王国名）
> 目标：上帝视角交互范式落地（点选/框选/右键指令），InputManager 2D 化，CharacterCreation 删除 rulerName 改 kingdomName，外围流程 UI 直迁，微操指令入口（D115/D116）架设
> 边界：右键指令派发（优先采集/守卫部署/移动/跟进）只架入口，具体消费归 2_8；摄像机拖拽缩放手感归 2_10；**建造 UI 施工进度展示归 2_12（D117 已移交 2_12 落地，D256，本篇仅提供交互入口——与设计稿 §3.1 F8 一致，2026-08-14 终审修订，D2XX）**；设置面板（D240）/倍速入口（D241）/信息面板（D118）归本篇落地（D256，不再推 2_10）

---

## 零、前置检查（不满足则停）

- [ ] 2_3 已落地：`PathFollower` 五态状态机 + `GridCoord` 目标寻路跑通
- [ ] 2_8 已落地或至少接口定义就绪：`TaskScheduler`/`FormationController` 可消费右键指令
- [ ] 2_2 已落地：`BuildController` 可消费 `InputMode.Build` + 放置交互
- [ ] 2_10 已落地或至少接口定义就绪：`CameraRig` pan/zoom 接口可调用
- [ ] 2_11 已落地或至少接口定义就绪：`GameSaveSummary` 含 `kingdomName` 字段
- [ ] 旧 `PlayerInputHandler.cs` 已标记退役（2_3 §2.2 声明）

---

## 一、实施步骤（按依赖序，每步可独立验收）

### 步骤 1：InputManager 上帝视角改造

**文件**：`Core/InputManager.cs` + `Resources/Config/Input/GameInput.inputactions`

**旧代码**：
- `InputManager` 绑定 `GameInput.Player.move/fastmove/esc/togglebuildmenu` 四个 Action
- 旧 `InputMode` 枚举：`Normal/Build/Dialog` 保留
- 旧 `PlayerMoveEvent` 发布（WASD 驱动君主移动）

**改造内容**：

1. **GameInput.inputactions 扩展**：新增 Action
   - `leftClick`（Button）：`<Mouse>/leftButton`，左键点击/框选
   - `rightClick`（Button）：`<Mouse>/rightButton`，右键指令
   - `middleDrag`（Button）：`<Mouse>/middleButton`，中键拖拽摄像机 pan
   - `scroll`（Value, Vector2）：`<Mouse>/scroll`，滚轮缩放

2. **InputManager.cs 改造**：
   ```csharp
   // 新增输入事件（框架不变，绑定新 Action）
   // 左键：发布 LeftClickPressedEvent（含屏幕坐标）
   // 右键：发布 RightClickPressedEvent（含屏幕坐标）
   // 中键拖拽：发布 CameraPanEvent（delta 屏幕像素）
   // 滚轮：发布 CameraZoomEvent（scrollDelta.y）
   ```

3. **移除旧 WASD 输入**：
   - `OnMove`/`OnFastMove` 回调**移除**（无君主可操控）
   - `PlayerMoveEvent` 相关代码**移除**
   - `MoveInput`/`RunHeld` 属性**移除**
   - `IsMovementEnabled` 属性**保留**（Build/Dialog 模式禁交互）

4. **InputMode 保留**：
   ```csharp
   // 保留 Normal/Build/Dialog 三种模式，语义不变
   // 保留 SetMode(InputMode) 接口
   // 保留 IsMovementEnabled → 改为 IsInteractionEnabled（Build/Dialog 禁交互点击）
   public bool IsInteractionEnabled => CurrentMode == InputMode.Normal;
   ```

**验收**：
- [ ] GameInput 新增 Action 绑定正确，编译通过
- [ ] 左键点击发布 `LeftClickPressedEvent`，右键点击发布 `RightClickPressedEvent`
- [ ] 中键拖拽和滚轮触发对应事件
- [ ] 旧 WASD 移动事件不再发布
- [ ] `InputMode` 切换机制正常工作

### 步骤 2：InteractionManager 2D 正交改造

**文件**：`Systems/Interaction/InteractionManager.cs`

**旧代码**：
- `_cam.ScreenToWorldPoint()` 用 2D OverlapPoint 检测命中
- `HandleClick()` 只响应左键点击 → `IInteractable.Interact()` → UI/动作
- 悬停高亮：`UpdateHover()` 改变 `SpriteRenderer.color`
- Build/Dialog 模式禁交互

**改造内容**：

1. **射线改造**：保留 2D 正交射线检测（`Camera.main.ScreenToWorldPoint` → `Physics2D.OverlapPoint`），CameraRig 2_10 提供正确的 2D 正交摄像机
2. **点击派发改造**：
   ```csharp
   // 左键点击 → 发布 LeftClickPressedEvent（含屏幕坐标 worldPos）
   // 保留旧 IInteractable → InteractionResult 派发（开 UI/动作/子菜单）
   // 保留点空白关面板
   // 框选不在此处处理（SelectionController 订阅 LeftClickPressedEvent）
   ```
3. **悬停高亮保留**：SpriteRenderer.color 高亮保留（`UpdateHover` 逻辑不变）
4. **右键派发移除**：右键全部由 `InputManager` → `LeftClickPressedEvent` / `RightClickPressedEvent` 驱动，`InteractionManager` 只处理左键点击交互，不处理右键

**验收**：
- [ ] 左键点击 `IInteractable` → 面板打开/动作执行，同旧行为
- [ ] 点空白 → 关面板
- [ ] 悬停高亮正常
- [ ] Build/Dialog 模式禁交互

### 步骤 3：SelectionController 新增（上帝视角核心）

**文件**：新增 `Systems/Interaction/SelectionController.cs`

**设计 §5.2**：
```csharp
public class SelectionController : Singleton<SelectionController>
{
    public List<UnitController> Selected { get; }   // 框选/点选集合（己方）
    public Building SelectedBuilding { get; }        // 点选建筑（独立于单位选择）
    public void BeginDrag(Vector2 screenPos);        // 左键按下（候选框选）
    public void EndDrag(Vector2 screenPos);          // 左键抬起 → 判定点击/框选
    public void IssueRightClick(Vector2 worldPos);   // 右键指令（归 2_8）
    public void DeployGuard(GridCoord target);       // 守卫部署（D116）
    public void PrioritizeHarvest(GridCoord resource);// 微操（D115）
}
```

**实现逻辑**：
- 订阅 `LeftClickPressedEvent` / `RightClickPressedEvent`（InputManager 发布）
- 框选阈值：`SelectionConfig.dragThresholdPx`（SO，默认 5px）
  - 拖拽距离 ≤ 阈值 → 点选（射线找最近可交互物）
  - 拖拽距离 > 阈值 → 框选（屏幕矩形 → 世界矩形 → `GridSystem.GetUnitsInRect` → 过滤 faction=己方）
- 框选矩形：`new Rect(dragStart, dragEnd - dragStart)` 屏幕像素
- 点选：优先 `IInteractable`（建筑/资源点），回落 `IClickInteractable`（NPC）
- 右键指令：
  - 有选中集 → 发布 `UnitCommandEvent(BattleCommand.MoveTo, worldPos)` 等（2_8 消费）
  - **右键点己方单位 = 跟进（Follow：跟随目标单位移动，D2，2026-08-14 终审修订，D2XX）**
  - 空选 → 清除选择
- 微操（D115）：选中工人 + 右键资源点 → `PrioritizeHarvest(GridCoord)`
- 守卫部署（D116）：选中士兵 + 右键高价值点 → `DeployGuard(GridCoord)`

**框中可视化**：`OnGUI` 或 `Debug.DrawLine` 临时画框选矩形（阶段 0 占位，正式渲染归 2_10）

**验收**：
- [ ] 左键点单位 → 选中 + 高亮；点空白 → 取消选择
- [ ] 拖拽框选多单位，只收己方
- [ ] 右键选中单位 → 发布 `UnitCommandEvent`
- [ ] 空选右键 → 清除选择
- [ ] 框选阈值可调（SO 5px）

### 步骤 4：CharacterCreationPanel 改造（rulerName→kingdomName）

**文件**：`Systems/UI/CharacterCreationPanel.cs` + `Core/NewGameConfig.cs`

**旧代码**：
- `CharacterCreationPanel`：`_nameInput`（rulerName）、`_difficultySelect`、`_mapSizeSelect`、`_worldSeedInput`
- `NewGameConfig`：`rulerName`、`mapSeed`、`difficulty`、`selectedSlotId`、`worldSeed`、`worldSize`
- `OnConfirmClicked()`：`rulerName = name` → `NewGameConfig { rulerName = name, ... }`

**改造内容**：

1. **NewGameConfig.cs**：
   ```csharp
   [System.Serializable]
   public class NewGameConfig
   {
       // === 删除 ===
       // public string rulerName = "无名君主";  // 删除
       public int mapSeed;    // 保留
       public int difficulty = 2;    // 保留
       public string selectedSlotId = "slot_1";  // 保留
       public int worldSeed;     // 保留
       public WorldSize worldSize = WorldSize.Medium; // 保留
       
       // === 新增 ===
       public string kingdomName = "河谷王国";  // 王国名（可选，默认）
   }
   ```

2. **CharacterCreationPanel.cs**：
   - `_nameInput`（王者名输入框）→ 改为 `_kingdomNameInput`（王国名输入框）
   - UI 布局：删除"君主名"输入，改为"王国名"输入（可选，默认"河谷王国"）
   - `OnConfirmClicked()`：
     ```csharp
     string kingdomName = string.IsNullOrEmpty(_kingdomNameInput.value) ? "河谷王国" : _kingdomNameInput.value;
     var config = new NewGameConfig
     {
         kingdomName = kingdomName,
         difficulty = difficulty,
         selectedSlotId = slotId,
         worldSeed = worldSeed,
         worldSize = worldSize,
         mapSeed = worldSeed != 0 ? worldSeed : UnityEngine.Random.Range(1, int.MaxValue)
     };
     ```

3. **MainMenuController.cs** 改造：
   - `OnCharacterCreateConfirmed()`：`config.rulerName` → `config.kingdomName`
   ```csharp
   Debug.Log($"[MainMenu] 新建游戏: kingdom={config.kingdomName}, slot={config.selectedSlotId}");
   ```

4. **WorldSystem.cs** 改造：
   - 旧 `RulerController.Instance.SetRulerName(newConfig.rulerName)` → 移除
   - 王国名写入 `KingdomSaveData.kingdomName`（2_11 联动）

**验收**：
- [ ] 新建游戏界面：无君主名输入，只有王国名（可选，默认"河谷王国"）+ 难度/地图大小/种子
- [ ] 确认后 `NewGameConfig.kingdomName` 正确填充

### 步骤 5：外围流程 UI 直迁（D238~D241）

**文件**：`Systems/UI/MainMenuPanel.cs`、`Systems/UI/SplashPanel.cs`、`Systems/UI/SaveSlotPanel.cs`、`Systems/UI/SaveSlotItem.cs`、`Systems/UI/PausePanel.cs` 直迁

**旧代码**：
- `MainMenuPanel`：4 按钮（continue/new-game/settings/quit），settings 未实现
- `SplashPanel`：3s 超时/任意键跳过
- `SaveSlotPanel`：3 槽位，显示 `rulerName` + 天 + 时间
- `SaveSlotItem`：`rulerName` 从 `meta.summary.rulerName` 读
- `PausePanel`：Esc 暂停/恢复，按钮（resume/save/quit），`timeScale` 管理

**改造内容**：

1. **MainMenuPanel**：直迁。**设置面板（D240）本篇实现：音量/语言/快捷键（归本篇 D256，2026-08-14 终审修订，D2XX——不再推 2_10）**
2. **SplashPanel**：直迁，零改动
3. **SaveSlotPanel + SaveSlotItem**：
   - `SaveSlotItem` 的 `rulerName` → 改为 `kingdomName`
   ```csharp
   // 旧：meta.summary.rulerName
   // 新：meta.summary.kingdomName（2_11 联动，GameSaveSummary 新增字段）
   string kingdomName = string.IsNullOrEmpty(meta.summary?.kingdomName) ? "未知王国" : meta.summary.kingdomName;
   metaLabel.text = $"{kingdomName} · 第{day}天 · {meta.saveTime}";
   ```
4. **PausePanel**：直迁。落地项（D256，2026-08-14 终审修订，D2XX）：
   - 设置面板（D240）：音量/语言/快捷键 + 倍速选项（0.5x/1x/2x/3x）——**本篇实现（归本篇 D256，不再推 2_10）**
   - 倍速入口（D241）本篇实现：UI 角落 0.5x/1x/2x/3x 按钮→调 2_8 SetGameSpeed（D256）；sim 对拍锁定 1x

**验收**：
- [ ] 主菜单流程完整：Splash→MainMenu→NewGame/Continue→SaveSlots/CharacterCreation→GameScene
- [ ] 存档槽摘要显示"王国名 · 第 N 天"
- [ ] Esc 暂停/恢复正常
- [ ] 3 个面板（Splash/MainMenu/Pause）旧行为零改动

### 步骤 6：SaveSlot / GameSaveSummary kingdomName 字段联动

**文件**：`Systems/Save/SaveRoot.cs`、`Systems/Save/SaveManager.cs`

**旧代码**：
```csharp
[Serializable]
public class GameSaveSummary
{
    public string rulerName;       // 旧：统治者名字
    public int currentDay;
    public int currentSeason;
    public int difficulty;
}
// SaveManager 保存时：
// summary.rulerName = RulerController.Instance.RulerName;  // 旧
```

**改造内容**：
```csharp
[Serializable]
public class GameSaveSummary
{
    public string rulerName;       // 保留（向后兼容旧档，读档时忽略）
    public string kingdomName;     // 新增：王国名
    public int currentDay;
    public int currentSeason;
    public int difficulty;
}
// SaveManager 保存时：
// summary.rulerName 不再写入（或留空）
// summary.kingdomName = KingdomManager.Instance.KingdomName;  // 从王国系统读
```

**验收**：
- [ ] 存档时 `summary.kingdomName` 正确写入
- [ ] 读档时 `summary.kingdomName` 正确恢复
- [ ] 旧档 `summary.rulerName` 不崩溃（2_11 迁移器处理）

### 步骤 7：OverheadSpeechManager 2D 坐标适配

**文件**：`Systems/Interaction/OverheadSpeechManager.cs`

**旧代码**：
- `worldPos` 为 `Vector2` 世界坐标，已是 2D 兼容
- `IsInView()` 用 `Camera.main.WorldToViewportPoint` 判断视野裁剪（2D 已兼容）
- `OverheadSpeech.Show()` 在 Transform 子物体创建 TextMesh 气泡（2D 兼容）

**改造内容**：
- 零逻辑改动，只验证坐标适配正确
- 确认 2D 正交摄像机下 `WorldToViewportPoint` 返回正确
- 气泡锚单位头顶（2D 世界坐标），`localPosition` 的 LOCAL_Y_OFFSET 保留

**验收**：
- [ ] 头顶气泡在 2D 正交摄像机下位置正确
- [ ] 视野裁剪正常（视野外 NPC 不冒泡）

### 步骤 8：WorkerInventory 归属补全

**文件**：`Systems/Unit/WorkerInventory.cs`

**旧代码**：
```csharp
public class WorkerInventory : MonoBehaviour
{
    public ResourceType carriedType;
    public int carriedAmount;
    public bool IsEmpty => carriedAmount <= 0;
    public bool IsFull => carriedAmount >= GetCarryCapacity();
    public int GetCarryCapacity() { /* 从 ResourceCarryConfig SO 读 */ }
    public int TryStore(ResourceType type, int amount) { /* 单资源背包 */ }
    public int UnloadAll() { /* 清空返回 */ }
}
```

**改造内容**：
- 零逻辑改动（WorkerInventory 是空间无关组件，搬运链 2D 化归 2_8/2_12）
- 仅确认坐标接缝：
  - 背包满提示位置（若有）→ 2D 世界坐标
  - 背包 UI 显示（若有）→ 2D UI 坐标
- 2D 距离计算（工人到仓库/目标点）由 2_8 任务调度处理

**验收**：
- [ ] `WorkerInventory` 编译通过，旧逻辑正常运行
- [ ] 背包满提示位置正确（2D 世界坐标）

### 步骤 9：直迁清单确认（空间无关系统）

**文件**：以下文件**零改动**直迁，仅验证引用链完整

| 文件 | 处置 | 验证项 |
|------|------|--------|
| `Systems/Unit/UnitDataManager.cs` | 直迁 | SO 数据层，编译通过 |
| `Systems/GameState/GameStateManager.cs` | 直迁 | 状态机，编译通过 |
| `Systems/Difficulty/DifficultyManager.cs` | 直迁 | 难度系数，编译通过 |
| `Systems/Time/TimeManager.cs` | 直迁 | 时间系统，编译通过 |
| `Systems/Loading/LoadManager.cs` | 直迁 | 加载流程，编译通过 |
| `Systems/Loading/TeardownManager.cs` | 直迁 | 销毁流程，编译通过 |
| `Core/EventBus.cs` | 直迁 | 事件总线，编译通过 |
| `Core/GameEvents.cs` | 直迁（移除外） | 移除 `PlayerMoveEvent`（君主移动事件退役） |
| `Systems/UI/UIManager.cs` | 直迁 | UI 栈，编译通过 |
| `Systems/UI/IUIPanel.cs` | 直迁 | 接口，编译通过 |
| `Systems/UI/IUIStackEntry.cs` | 直迁 | 接口，编译通过 |
| `Systems/UI/GameOverPanel.cs` | 直迁 | 面板逻辑，编译通过 |
| `Systems/UI/LoadingPanel.cs` | 直迁 | 面板逻辑，编译通过 |
| `Systems/UI/ResourceHUD.cs` | 直迁 | 资源 HUD，编译通过 |
| `Systems/UI/TopLeftHUD.cs` | 直迁 | 左上 HUD，编译通过 |
| `Systems/UI/TradePanel.cs` | 直迁 | 贸易面板，编译通过 |
| `Systems/UI/TrainingPanel.cs` | 直迁 | 训练面板，编译通过 |
| `Systems/UI/WarehousePanel.cs` | 直迁 | 仓库面板，编译通过 |
| `Systems/UI/BuildingPanel.cs` | 直迁 | 建筑面板，编译通过 |
| `Systems/UI/BuildingMenuPanel.cs` | 直迁 | 建造菜单面板，编译通过 |
| `Systems/UI/FormationPanel/FormationPanel.cs` | 直迁 | 编队面板，编译通过 |
| `Systems/UI/PopulationPanel.cs` | 直迁 | 人口面板，编译通过 |
| `Systems/UI/UIDragHelper.cs` | 直迁 | 拖拽辅助，编译通过 |
| `Systems/UI/MainMenuController.cs` | 直迁（改造引用） | 流程控制器，编译通过 |
| `Systems/Interaction/IInteractable.cs` | 直迁 | 接口，编译通过 |
| `Systems/Interaction/IClickInteractable.cs` | 直迁 | 接口，编译通过 |

**验收**：
- [ ] 以上文件全部编译通过
- [ ] `PlayerMoveEvent` 从 `GameEvents.cs` 移除（或标记退役）
- [ ] 旧引用 `PlayerMoveEvent` 的代码已清理

### 步骤 10：PlayerInputHandler / RulerController 退役

**文件**：`Systems/Unit/PlayerInputHandler.cs` → **删除**；`Systems/Ruler/RulerController.cs` → **职责迁移**

**旧代码**：
- `PlayerInputHandler`：读取 `InputManager.MoveInput` → `UnitController.Move`
- `RulerController`：君主控制器（移动/资源池/HP/GameOver）

**改造内容**：
1. **删除 `PlayerInputHandler.cs`**：从 Unity 场景中移除挂载，删除文件
2. **RulerController 退役**：
   - 君主移动/操控逻辑全部移除
   - 资源池职责（木/石/粮/金全局资源）→ 迁移到仓库系统（2_12 §2.4）
   - HP/GameOver → 主城建筑判定（2_12 D149）
   - 文件保留但标记 `[Obsolete]`，待 2_12 完成资源池迁移后整体删除

**验收**：
- [ ] `PlayerInputHandler.cs` 已删除，编译无残留引用
- [ ] `RulerController` 不再处理移动输入

### 步骤 11：微操指令入口架设（D115/D116）

**文件**：`Systems/Interaction/SelectionController.cs`（步骤 3 基础上扩展）

**D115 微操——框选工人→右键资源点=优先采集/守卫**：
```csharp
public void PrioritizeHarvest(GridCoord resource)
{
    // 筛选选中集：仅 Worker 职业
    var workers = Selected.Where(u => u.Data.occupation == Occupation.Worker).ToList();
    if (workers.Count == 0) return;
    
    // 发布优先采集指令（2_8 TaskScheduler 消费）
    EventBus.Publish(new PrioritizeHarvestCommand(workers, resource));
    // 清空选中（指令已发）
    ClearSelection();
}
```

**D116 守卫部署——选中士兵→右键高价值点=派兵守卫**：
```csharp
public void DeployGuard(GridCoord target)
{
    // 筛选选中集：仅士兵职业（Warrior/Archer/Mage/Healer/Cavalry/General）
    var soldiers = Selected.Where(u => IsSoldier(u.Data.occupation)).ToList();
    if (soldiers.Count == 0) return;
    
    // 发布守卫部署指令（2_8 GuardDeployment 消费）
    EventBus.Publish(new GuardDeployCommand(soldiers, target));
    // 清空选中
    ClearSelection();
}
```

**右键指令分派**：
```csharp
public void IssueRightClick(Vector2 worldPos)
{
    GridCoord coord = GridSystem.WorldToCoord(worldPos);

    // 1. 检测右键目标
    // 2. 若选中集是工人 + 目标是资源点 → PrioritizeHarvest
    // 3. 若选中集是士兵 + 目标是高价值点 → DeployGuard
    // 4. 若目标是己方单位 → Follow（跟进：跟随目标单位移动，D2，2026-08-14 终审修订，D2XX）
    // 5. 否则 → 移动指令（MoveTo）
    // 6. 空选 → 取消选择
}
```

**验收**：
- [ ] 框选工人 + 右键资源点 → `PrioritizeHarvestCommand` 事件发布
- [ ] 选中士兵 + 右键高价值点 → `GuardDeployCommand` 事件发布
- [ ] 选中单位 + 右键空地 → `UnitCommandEvent(MoveTo)` 发布
- [ ] 右键点己方单位 → Follow（跟进）指令发布（D2，2026-08-14 终审修订，D2XX）
- [ ] 空选右键 → 清除选择

### 步骤 11B：D118 信息面板完善（D256，2026-08-14 终审修订，D2XX）

**文件**：`Systems/UI/BuildingPanel.cs`（或独立 InfoPanel）+ 数据源 2_12/2_8

信息面板补三字段：仓库存量（2_12 IWarehouse.Query）/ 守卫状态（IsGuarded，2_8 GuardDeployment）/ 威胁等级（2_8/2_7 热度口径，价值驱使 D56）。归本篇落地（D256）。

**验收**：
- [ ] 点选建筑 → 信息面板显示仓库存量/守卫状态（IsGuarded）/威胁等级三字段（数据源 2_12/2_8）

### 步骤 11C：P1 输入档（D274，2026-08-14 终审修订，D2XX）

**文件**：`Core/InputManager.cs` + `GameInput.inputactions` + `Systems/Interaction/SelectionController.cs`

P1 档输入：Shift+点击=加选/减选、Ctrl+数字键 1~9=编队保存、数字键=编队调用、B=建造菜单、R=训练菜单；P0 仅基础键（左/右/中键/滚轮/E/ESC）。

**验收**：
- [ ] Shift+点击加选/减选生效；Ctrl+数字 1~9 保存编队、数字键调用编队；B 开建造菜单、R 开训练菜单

---

## 二、涉及文件清单

| 文件 | 改动 |
|------|------|
| `Core/InputManager.cs` | 改造：移除 WASD 输入，新增左键/右键/中键/滚轮绑定 |
| `Resources/Config/Input/GameInput.inputactions` | 改造：新增 leftClick/rightClick/middleDrag/scroll Action |
| `Systems/Interaction/InteractionManager.cs` | 改造：2D 正交射线适配，右键移除，框选解耦 |
| `Systems/Interaction/SelectionController.cs` | **新增**：上帝视角点选/框选/右键指令核心 |
| `Systems/UI/CharacterCreationPanel.cs` | 改造：rulerName→kingdomName |
| `Core/NewGameConfig.cs` | 改造：rulerName 删除，kingdomName 新增 |
| `Systems/UI/MainMenuController.cs` | 改造：rulerName→kingdomName 引用更新 |
| `Systems/UI/SaveSlotItem.cs` | 改造：rulerName→kingdomName 显示 |
| `Systems/UI/SaveSlotPanel.cs` | 直迁（零改） |
| `Systems/UI/MainMenuPanel.cs` | 直迁（零改） |
| `Systems/UI/SplashPanel.cs` | 直迁（零改） |
| `Systems/UI/PausePanel.cs` | 直迁（零改） |
| `Systems/Save/SaveRoot.cs` | 改造：GameSaveSummary 新增 kingdomName 字段 |
| `Systems/Save/SaveManager.cs` | 改造：保存时写 kingdomName（非 rulerName） |
| `Systems/Interaction/OverheadSpeechManager.cs` | 验证（坐标适配） |
| `Systems/Unit/WorkerInventory.cs` | 验证（零改，确认接缝） |
| `Systems/Unit/PlayerInputHandler.cs` | **删除** |
| `Systems/Ruler/RulerController.cs` | 标记退役，资源池职责迁移占位 |
| `Core/GameEvents.cs` | 改造：移除 PlayerMoveEvent |
| `Systems/Unit/UnitDataManager.cs` | 直迁（零改） |
| `Systems/GameState/GameStateManager.cs` | 直迁（零改） |
| `Systems/UI/UIManager.cs` | 直迁（零改） |
| `Systems/UI/IUIPanel.cs` | 直迁（零改） |
| `Systems/UI/IUIStackEntry.cs` | 直迁（零改） |
| （其余直迁面板 11 个） | 直迁（零改） |

---

## 三、SO 配置

### SelectionConfig（新增）

| 字段 | 类型 | 默认 | 说明 |
|------|------|------|------|
| dragThresholdPx | int | 5 | 框选/点击判定阈值（像素），拖拽 > 阈值判框选 |
| onlyFriendly | bool | true | 框选仅收己方单位（faction 过滤） |
| highlightColor | Color | Color.yellow | 选中单位高亮色 |

### KingdomConfig（追加）

| 字段 | 类型 | 默认 | 说明 |
|------|------|------|------|
| kingdomName | string | "河谷王国" | 默认王国名（存档/UI 显示） |

### GuardConfig（新增，占位）

| 字段 | 类型 | 默认 | 说明 |
|------|------|------|------|
| maxGuardPerPoint | int | 6 | 每高价值点守卫上限（D116） |

### BuildConfig（追加，占位）

| 字段 | 类型 | 默认 | 说明 |
|------|------|------|------|
| showConstructionProgress | bool | true | 施工进度 UI 开关（D117） |

---

## 四、冒烟验证 checklist（照单执行）

- [ ] 左键点单位 → 选中 + 面板打开；点空白 → 取消选择
- [ ] 拖拽框选 3 个工人 → 选中集为 3 个己方单位
- [ ] 右键选中工人 + 空地 → `UnitCommandEvent(MoveTo)` 发布
- [ ] 右键框选工人 + 资源点 → `PrioritizeHarvestCommand` 发布
- [ ] 右键选中士兵 + 高价值点 → `GuardDeployCommand` 发布
- [ ] 滚轮缩放 + 中键拖拽摄像机 → 事件正确发布
- [ ] 新建游戏：无君主名输入，仅王国名/难度/地图大小/种子 → `NewGameConfig.kingdomName` 正确
- [ ] 存档槽摘要显示"王国名 · 第 N 天"
- [ ] 主菜单流程全通：Splash→MainMenu→NewGame→CharacterCreation→SaveSlot→GameScene
- [ ] Esc 暂停 → 暂停菜单 → 继续/保存/退出 正常
- [ ] 头顶气泡在 2D 正交摄像机下位置正确
- [ ] 直迁清单 22 个文件编译通过

---

## 五、验收标准

1. 上帝视角交互完整：点选/框选/右键指令/建造模式互斥/摄像机拖拽缩放
2. InputManager 旧 WASD 移动移除，新增左键/右键/中键/滚轮事件绑定
3. SelectionController 点选+框选正确，仅收己方单位
4. CharacterCreationPanel 无君主名，王国名可选（默认"河谷王国"）
5. 外围流程 UI 直迁（主菜单/暂停/设置/存档选择）零改动跑通；**设置面板（D240）/倍速入口（D241）本篇实现（D256，不再推 2_10）：音量/语言/快捷键 + 0.5x/1x/2x/3x 倍速按钮→2_8 SetGameSpeed（2026-08-14 终审修订，D2XX）**
6. SaveSlot 摘要显示王国名（非统治者名）
7. 微操入口（D115）：框选工人→右键资源点→优先采集指令发布
8. 守卫部署入口（D116）：选中士兵→右键高价值点→派兵守卫指令发布
9. 头顶气泡/点击面板坐标 2D 正确
10. 直迁清单系统（GameState/难度/时间/加载/销毁/事件/UI 栈）零改动跑通

> **与设计稿 §六对齐（2026-08-14 终审修订，D2XX）**：D117 施工进度 UI **已移交 2_12（D256）**，本篇仅验收交互入口（点击建筑显示进度）；补充验收项——D118 信息面板（仓库存量/守卫状态/威胁等级三字段）、D240 设置面板、D241 倍速入口（见步骤 11B/步骤 5）。

---

## 六、风险与回滚

| 风险 | 对策 |
|------|------|
| 旧输入绑定（WASD 移动）被误删但仍有引用 | 全局搜 `PlayerMoveEvent`/`MoveInput`/`OnMove` 确保清理干净 |
| 框选/点击歧义（阈值不好调） | SO `dragThresholdPx` 可调，重启即生效 |
| 右键指令与旧右键功能冲突 | 设计已定：右键统一为"指令"，无选中时右键=取消选择（不触发平移，平移归中键） |
| 旧档 `rulerName` 字段 | 2_11 迁移器处理：rulerName 忽略/映射 kingdomName |
| UI 直迁面板 UXML 引用 | 面板逻辑直迁，但 UXML 中旧 `ruler-name-input` 需改为 `kingdom-name-input` |
| PlayerInputHandler 删除后引用残留 | 全局搜 `PlayerInputHandler`，确保场景/预制体引用清空后再删除 |