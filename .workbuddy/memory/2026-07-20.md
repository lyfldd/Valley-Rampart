# 2026-07-20 工作日志

## 架构重构 — 修复 41 个问题中的 P0/P1

### 删除（清理死代码和残留）
- 删除 `ResourcesLoader.cs`（死代码，方法名 Loda 拼错）
- 删除 `Resources_moved/`（Addressables 迁移残留）
- 删除 `AddressableAssetsData/`（Addressables 配置）
- 删除空的 `Resources/RulerData/` 和 `Resources/UI/`
- 删除 `RulerDataManager.cs`（旧 Addressables 管理器）
- 删除 `Systems/UnitFactory.cs`（旧 static 空壳）
- 删除 13 个空 Systems 子目录

### 重命名
- `RulerData.cs` → `_Game/Data/UnitData.cs`（从 Resources/Config/ 移出）
- `RulerDataManager.cs` → `_Game/Systems/Unit/UnitDataManager.cs`
- 字段 `runWalk` → `runSpeed`
- 事件 `RulerDataLoadedEvent` → `UnitDataLoadedEvent`

### 新增
- `Core/Singleton.cs` — MonoBehaviour 单例基类，统一生命周期
- `Systems/GameState/GameStateManager.cs` — GameState 枚举 + 状态切换 + 事件广播
- `Systems/Unit/UnitController.cs` — 单位运行时控制器（HP/伤害/治疗/死亡）
- `GameEvents.cs` 新增 `GameStateChangedEvent`
- `EventBus.cs` 新增无订阅者告警 + `HasSubscribers<T>()` 方法

### 重写
- `GameBootstrap.cs` — 状态驱动启动链：Booting → Loading → Ready → Playing
  - `[DefaultExecutionOrder(-100)]` 保证最先执行
  - 显式初始化所有 Manager、创建君主、启用输入
- `InputManager.cs` — 从纯 C# 类改为 MonoBehaviour 单例
  - 继承 Singleton<T>，去掉 IDisposable
  - 只有 Playing 状态才响应输入
  - Awake 初始化但不 Enable，等 GameBootstrap 显式调用
- `UnitDataManager.cs` — 从 Addressables 改为 Resources.LoadAll 同步加载
  - 新增单位 = 往 Resources/UnitData/ 扔 .asset 文件
- `UnitFactory.cs` — 从 static class 改为 MonoBehaviour 单例
  - PreloadAll() 同步预加载 Prefab
  - SpawnUnit() 接受 UnitData 或 Faction+Occupation

### 数据迁移
- `Human_Player_Ruler.asset` 从 `Resources_moved/RulerData/` 移到 `Resources/UnitData/`
- .asset 文件更新引用新 UnitData.cs 的 GUID

### 当前文件结构
```
_Game/
├── Core/
│   ├── EventBus.cs          ← 改进：无订阅者告警
│   ├── GameBootstrap.cs     ← 重写：状态驱动
│   ├── GameEvents.cs        ← 改：UnitDataLoadedEvent + GameStateChangedEvent
│   ├── InputManager.cs      ← 重写：MonoBehaviour 单例
│   └── Singleton.cs         ← 新
├── Data/
│   └── UnitData.cs          ← 重命名（原 RulerData）
└── Systems/
    ├── Camera/
    │   └── CameraSetup.cs   ← 仍待填充
    ├── GameState/
    │   └── GameStateManager.cs  ← 新
    ├── Ruler/
    │   └── RulerController.cs   ← 仍待填充
    └── Unit/
        ├── UnitController.cs     ← 新
        ├── UnitDataManager.cs    ← 重写
        └── UnitFactory.cs        ← 重写
```

### 仍待完成
- CameraSetup.cs 填充实际逻辑（设置 orthographicSize）
- RulerController.cs 填充移动逻辑
- 创建君主 Prefab（Resources/UnitPrefabs/Human_Player_Ruler.prefab）
- 场景中 _Managers 需要挂载 GameBootstrap（确保 GUID 正确）
